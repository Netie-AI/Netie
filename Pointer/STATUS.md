# STATUS

**Hard cap: 60 lines.** Older narrative moves to CHANGELOG.md.

## Now

1. Local app **is running** (`what_is_live.sh` 13:40 UTC). Cloud Run **not** deployed. Devpost **not** submitted. A win is **not** confirmed. Stripe skipped this tick (daily only).
2. Windows install fix: `install_windows.ps1` proves hardware and copies Desktop POINTER_PROVE.json **before** daemon health. Daemon down is a warning, not a throw. Drive still has no POINTER_PROVE.json. `Netie-AI/Pointer` still 404.
3. OpenClaw/Hermes not installed here. $1M is not in evidence.

## Next

- Founder: unzip Pointer/, double-click `scripts\\install_windows.cmd`, upload Desktop POINTER_PROVE.json even if the browser tabs fail.
- Hackathon: https://forms.gle/riGhgDSHkHeMx8Ca6 then key + `DEPLOY=1`. This agent cannot auto-join or auto-win.

## Later

- Clone product Pointer when visible. Goal stays open until $1M and laptop control are proven.
