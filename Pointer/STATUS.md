# STATUS

**Hard cap: 60 lines.** Older narrative moves to CHANGELOG.md.

## Now

1. Verify this tick: 14 units + 3 pair-card tests, live mouse, `/health`+`/pay`+`GET /`, gated sandbox write (`degraded: local_act_without_cortex`). `Netie-AI/Pointer` still GitHub 404.
2. Stripe live `acct_1RMx9FFV5wcFod2f`: charges 0, MYR 0 available / 0 pending. `charges_enabled=true`, `payouts_enabled=false` (`individual.verification.document` + business-model form past due).
3. No Slingshot confirmation mail. No inbound from Easyway/Hengxing. Outlier ToS accepted 11:15 (founder browser only; this agent has no IC). OpenClaw/Hermes not installed here.

## Next

- Founder laptop: `.\scripts\install_windows.ps1` then http://127.0.0.1:7420/ . Tokens stay on the laptop. Do not set POINTER_ALLOW_REMOTE=1.
- Stripe Dashboard: upload identity + business-model form. Pointer install SKU: https://buy.stripe.com/dRmaEXchg7Wr7nS8D29ws08
- Do not re-mail Easyway/Hengxing today. GMS CIP Spark after a second teammate is named.

## Later

- Clone product Pointer when visible. P-002 hardware proof. Bounty after `payouts_enabled=true`. $1M is not in evidence.
