# STATUS

**Hard cap: 60 lines.** Older narrative moves to CHANGELOG.md.

## Now

1. Verify measured 2026-08-22 12:31 UTC: 25 tests OK, live mouse, `/health`+`/pay`+QR+`GET /` 200, gated sandbox write. Linux prove ok=true (now requires screenshot bytes). Git `8a86e56`. `Netie-AI/Pointer` still 404. Stripe charges 0.
2. Windows prove now sets DPI-aware + clamps to screen size. Still no `POINTER_PROVE.json` from the laptop. Fiverr/H1/Slingshot empty.
3. OpenClaw/Hermes not installed here. Do not flip netie.ai apex DNS.

## Next

- Founder: Drive laptop pack `1by_5VEBbQpD86So-q6K8F2T2MpcHWNkd4BKb4UlARI0`, run `.\scripts\install_windows.ps1`, upload Desktop `POINTER_PROVE.json` only.
- GMS / Fiverr / H1 / Stripe identity. Do not re-blast Easyway/Hengxing today.

## Later

- Clone product Pointer when visible. Durable pay host (P-013). $1M is not in evidence.
