diff --git a/engine.js b/engine.js
index 53bdf96..ef3305f 100644
--- a/engine.js
+++ b/engine.js
@@ -96,6 +96,11 @@ function listedOptions(listed, value) {
   return want ? rows : [""].concat(rows);
 }
 
+function objectForSetPoint(objectType, objects) {
+  const listed = objects && typeof objects === "object" ? Object.keys(objects) : [];
+  return listedOrEmpty(objectType, listed);
+}
+
 function irError(ghost, message) {
   return {
     version: "1.0",
@@ -588,7 +593,8 @@ async function handleChat(raw) {
   if (setPoint) {
     const node = C.selected && C.selected();
     if (!node) return "Select a node first.";
-    const obj = node.object_type && C.OBJECTS[node.object_type] ? node.object_type : "inventory";
+    const obj = objectForSetPoint(node.object_type, C.OBJECTS);
+    if (!obj) return "Set object first. Unlabeled node does not invent inventory.";
     if (!C.OBJECTS[obj] || !C.OBJECTS[obj].points[setPoint[1]]) {
       return "Point not on " + obj + ". " + Object.keys((C.OBJECTS[obj] && C.OBJECTS[obj].points) || {}).join(", ");
     }
@@ -1165,5 +1171,6 @@ if (typeof module !== "undefined" && module.exports) {
     ghostWalk,
     listedOrEmpty,
     listedOptions,
+    objectForSetPoint,
   };
 }
diff --git a/tests/compiler.test.cjs b/tests/compiler.test.cjs
index 5ee9201..a7e6c1c 100644
--- a/tests/compiler.test.cjs
+++ b/tests/compiler.test.cjs
@@ -9,6 +9,7 @@ const {
   ghostWalk,
   listedOrEmpty,
   listedOptions,
+  objectForSetPoint,
 } = require("../engine.js");
 
 test("foundry path ranks orchestrator-subagent first", () => {
@@ -268,3 +269,13 @@ test("compileIR does not invent T0 for a missing tier", () => {
   assert.equal(ir.error, undefined);
   assert.equal(ir.nodes[0].tier, null);
 });
+
+test("set point does not invent inventory for a missing object", () => {
+  const objects = {
+    inventory: { points: { sku: "string" } },
+    shipments: { points: { dest: "string" } },
+  };
+  assert.equal(objectForSetPoint("", objects), "");
+  assert.equal(objectForSetPoint("nope", objects), "");
+  assert.equal(objectForSetPoint("inventory", objects), "inventory");
+});
