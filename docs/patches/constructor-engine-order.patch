diff --git a/engine.js b/engine.js
index 4f36586..947d729 100644
--- a/engine.js
+++ b/engine.js
@@ -1351,12 +1351,21 @@ function bindChat() {
   rankApproaches();
 }
 
-if (typeof document !== "undefined") {
-  if (document.readyState === "loading") {
-    document.addEventListener("DOMContentLoaded", bindChat);
-  } else {
-    bindChat();
+function bindWhenReady() {
+  if (typeof document === "undefined") return;
+  if (!window.Constructor) {
+    if (document.readyState === "loading") {
+      document.addEventListener("DOMContentLoaded", bindWhenReady);
+      return;
+    }
+    setTimeout(bindWhenReady, 0);
+    return;
   }
+  bindChat();
+}
+
+if (typeof document !== "undefined") {
+  bindWhenReady();
 }
 
 if (typeof module !== "undefined" && module.exports) {
@@ -1377,5 +1386,6 @@ if (typeof module !== "undefined" && module.exports) {
     notesDenied,
     nodesWithoutNotes,
     cortexPayload,
+    bindWhenReady,
   };
 }
diff --git a/index.html b/index.html
index 95b0562..5a82891 100644
--- a/index.html
+++ b/index.html
@@ -6,7 +6,7 @@
     <title>Constructor — Netie</title>
     <link rel="icon" href="favicon.ico" sizes="any" />
     <link rel="icon" href="favicon.svg" type="image/svg+xml" />
-    <link rel="stylesheet" href="styles.css?v=14" />
+    <link rel="stylesheet" href="styles.css?v=15" />
   </head>
   <body class="chat-open chat-snap-right">
     <header class="top">
diff --git a/tests/compiler.test.cjs b/tests/compiler.test.cjs
index b095438..48d0605 100644
--- a/tests/compiler.test.cjs
+++ b/tests/compiler.test.cjs
@@ -13,7 +13,10 @@ const {
   applyObjectType,
   cortexPayload,
   nodesWithoutNotes,
+  bindWhenReady,
 } = require("../engine.js");
+const fs = require("node:fs");
+const path = require("node:path");
 
 test("foundry path ranks orchestrator-subagent first", () => {
   const ranked = rankForKinds(["connector", "ontology", "insight", "foundry", "app"]);
@@ -790,3 +793,14 @@ test("applyObjectType clears a data_point not on the new object", () => {
   assert.equal(unlabeled.object_type, "");
   assert.equal(unlabeled.data_point, "");
 });
+
+test("index.html loads engine.js before app.js", () => {
+  const html = fs.readFileSync(path.join(__dirname, "..", "index.html"), "utf8");
+  const engineAt = html.indexOf("engine.js");
+  const appAt = html.indexOf("app.js");
+  assert.ok(engineAt >= 0 && appAt > engineAt);
+});
+
+test("bindWhenReady is a no-op without a document", () => {
+  bindWhenReady();
+});
