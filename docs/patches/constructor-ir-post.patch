diff --git a/engine.js b/engine.js
index e56528e..123c6af 100644
--- a/engine.js
+++ b/engine.js
@@ -217,7 +217,7 @@ function nodesWithoutNotes(nodes) {
 }
 
 function cortexPayload(state) {
-  // compileIR is the gate. Raw canvas never POSTs to Cortex.
+  // compileIR is the gate and the body. Raw canvas never POSTs to Cortex.
   const ir = compileIR(state || {});
   if (ir.error) {
     return { error: ir.error, body: null };
@@ -225,8 +225,13 @@ function cortexPayload(state) {
   return {
     error: undefined,
     body: {
-      nodes: nodesWithoutNotes((state && state.nodes) || []),
-      edges: ((state && state.edges) || []).slice(),
+      version: ir.version,
+      engine: ir.engine,
+      ghost: ir.ghost,
+      entry_node_id: ir.entry_node_id,
+      output_node_id: ir.output_node_id,
+      nodes: ir.nodes,
+      edges: ir.edges,
     },
   };
 }
diff --git a/tests/compiler.test.cjs b/tests/compiler.test.cjs
index 48d0605..08d75c3 100644
--- a/tests/compiler.test.cjs
+++ b/tests/compiler.test.cjs
@@ -717,6 +717,8 @@ test("cortexPayload strips notes and does not POST a leaked body", () => {
     edges: [],
   });
   assert.equal(packed.error, undefined);
+  assert.equal(packed.body.engine, "cortex");
+  assert.equal(packed.body.nodes[0].kind, "DOCUMENT_REF");
   assert.equal("note" in packed.body.nodes[0], false);
   const leak = cortexPayload({
     nodes: [{ id: "c", kind: "connector", note: "skill_body: SECRET" }],
@@ -726,6 +728,41 @@ test("cortexPayload strips notes and does not POST a leaked body", () => {
   assert.equal(leak.body, null);
 });
 
+test("cortexPayload POSTs compileIR not the canvas", () => {
+  const packed = cortexPayload({
+    nodes: [
+      {
+        id: "c",
+        kind: "connector",
+        object_type: "hr_notes",
+        data_point: "salary",
+        action_type: "bash",
+        x: 9,
+        note: "in",
+      },
+    ],
+    edges: [],
+  });
+  assert.equal(packed.error, undefined);
+  const n = packed.body.nodes[0];
+  assert.equal(n.kind, "DOCUMENT_REF");
+  assert.equal(n.constructor_kind, "connector");
+  assert.equal(n.object_type, null);
+  assert.equal(n.data_point, null);
+  assert.equal(n.action_type, null);
+  assert.equal("x" in n, false);
+  assert.equal("note" in n, false);
+  assert.equal(n.requires_confirm, false);
+  const write = cortexPayload({
+    nodes: [{ id: "t", kind: "tool_call", action_type: "export_pptx" }],
+    edges: [],
+  });
+  assert.equal(write.error, undefined);
+  assert.equal(write.body.nodes[0].kind, "TOOL_CALL");
+  assert.equal(write.body.nodes[0].action_type, "export_pptx");
+  assert.equal(write.body.nodes[0].requires_confirm, true);
+});
+
 test("cortexPayload refuses a disconnected graph before Cortex", () => {
   const packed = cortexPayload({
     nodes: [
