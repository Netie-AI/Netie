diff --git a/engine.js b/engine.js
index 68f0863..c4877a4 100644
--- a/engine.js
+++ b/engine.js
@@ -184,6 +184,10 @@ function compileIR(state) {
   if (seen !== nodes.length) {
     return irError(ghost, "cycle");
   }
+  const order = topo({ nodes: nodes, edges: edges });
+  if (!order.length) {
+    return irError(ghost, "no entry");
+  }
   for (let i = 0; i < nodes.length; i++) {
     const n = nodes[i];
     if (n.kind !== "tool_call") continue;
@@ -200,7 +204,7 @@ function compileIR(state) {
     version: "1.0",
     engine: "cortex",
     ghost: ghost,
-    entry_node_id: nodes[0].id,
+    entry_node_id: order[0],
     output_node_id: output.id,
     nodes: nodes.map((n) => {
       let kind = CORTEX_KIND[n.kind];
diff --git a/tests/compiler.test.cjs b/tests/compiler.test.cjs
index 6591689..f930e27 100644
--- a/tests/compiler.test.cjs
+++ b/tests/compiler.test.cjs
@@ -67,6 +67,19 @@ test("topo does not append leftover cyclic nodes", () => {
   assert.deepEqual(cycle, []);
 });
 
+test("compileIR entry is the Kahn source, not array[0]", () => {
+  const ir = compileIR({
+    nodes: [
+      { id: "sink", kind: "app", note: "out" },
+      { id: "src", kind: "connector", note: "in" },
+    ],
+    edges: [{ from: "src", to: "sink" }],
+  });
+  assert.equal(ir.error, undefined);
+  assert.equal(ir.entry_node_id, "src");
+  assert.equal(ir.output_node_id, "sink");
+});
+
 test("verify path ranks generator-verifier first", () => {
   const ranked = rankForKinds(["hypothesize", "audit"]);
   assert.equal(ranked[0].id, "generator_verifier");
