# Send the domain root to payable hire work.
# Keep /aim/, /asa/, /projects/, /hire/, and Next.js assets on their own URLs.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /hire/ [R=302,L]
</IfModule>
