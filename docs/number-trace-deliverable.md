# Source trace deliverable

Use this only for an agreed local-file analysis. The helper reads the selected file and does not upload it.

```
python scripts/trace_number.py FILE CELL
```

Example:

```
python scripts/trace_number.py docs/fixtures/sample-outbound.xlsx WK32!F4
```

Return the helper's exact output as email body or a `.txt` attachment. Do not guess. Keep `Cannot be traced` when the selected cell has no verifiable supported formula.

---

NETIE SOURCE TRACE
Date:
Buyer:
File name:
Sheet:

Figure asked:
Value (raw; display format not applied):
Formula:

Inputs:
- Cell and raw value for each resolved input

Recomputed from inputs:
Verdict:
- Traced / Cannot be traced
Reason:

Notes (only what the file shows):

I only used the local file you selected.

Jian Hong / Netie / Penang
oojianhongg@gmail.com
