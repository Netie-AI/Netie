# Invoice ETL workbench deployment finding

- Date: 2026-08-23 UTC
- Model: computer-use agent plus local byte compare
- Scope: Replace the live cited-answer Q&A toy with a local invoice ETL workbench and full-width hire pages
- Keywords: ETL, invoice cleaning, lineage, exceptions, vendor rollup, File Manager, viewport, catalog.json, llms.txt
- Main idea: File Manager published a 20-row synthetic cleaning job that fills the viewport; agents can read catalog.json and llms.txt. It is still a local sample, not a production warehouse.

## Verified result

- Uploaded eight files through Spaceship File Manager only.
- Production downloads of hire, knowledge, ops, trace, catalog.json, llms.txt, robots.txt, and sitemap.xml matched the committed source byte-for-byte.
- Knowledge title is `Invoice extract workbench | Netie.AI`.
- Job totals: 20 raw / 11 kept / 3 duplicates / 6 quality exceptions / 55% keep rate / RM 9241.25.
- Browser clicks: Failed rows (R04, R07, R11), Duplicates (R02, R06, R13), Pen Express RM 3855.25, unknown question no-answer.
- Wide layout: hire, knowledge, and ops fill the window instead of a skinny center column.
- BrewPage, Surge, Telegraph, and Rentry mirrors now describe the ETL sample, not the old Q&A toy.

## Public boundary

Synthetic records only. No upload, model, warehouse, customer file, login, or retrieval backend. The catalog is a read-only offer list for humans and agents; written scope still happens before payment.
