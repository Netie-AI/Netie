# PRD-001 - Governed answers over your own spreadsheets and databases

**Product:** DMS / Spaces
**Owner:** founder
**Status:** active - sliced; founder amendments 2026-08-03
**Repos in scope:** `Netie-AI/dms`, `Netie-AI/Cortex`
**Created:** 2026-08-02
**Amended:** 2026-08-03 (multi-turn follow-ups; EPIC-008 pull-forward; Spaces store=B; F10–F24 L2 / insights / MCP / RAG / Space-UI / AirGPT-bench / dual-demo); 2026-08-03 (F25: Excel-native Copilot orchestration EPIC-016; Serves AirGPT F15); 2026-08-03 (F25 NEEDS-YOU resolved: Pointer-primary + Copilot YES; DMS owns prompt-pack cross-check); 2026-08-05 (F27 NEEDS-YOU closed: decline Cassandra/MCP-in-customer-DB; F28 accuracy research maps Wave 7 / P-DMS-29); 2026-08-05 (F29: What's New + guided tour EPIC-023 deferred SURFACE; AIP Build-examples / Workshop-class backends stay H6); 2026-08-05 (F30: Palantir SuperRepo beta → EPIC-022 / Cortex P14–P17 dual-brain + packs; Foundry CLI/Marketplace clone stays H6 / P-DMS-31; not EPIC-023); 2026-08-05 (F31: adaptive architecture chooser → Cortex gen-cFSM/P21 research; Wave 7 EPIC-017/018 stays land-for-test; park P-DMS-32); 2026-08-05 afternoon (F29 NEEDS-YOU closed **A**: thin EPIC-023 only; B/H6 amend rejected; EPIC-023 approved deferred not RUN NOW; accuracy = residual 017/018 then 019); 2026-08-05 (F32: DMS ask reproduces F26 Wide_Fill totals; maps open EPIC-017/018/019 — do not reopen SCORE; Cursor chrome OOS); 2026-08-07 (**F36 direction shift - NEEDS-YOU OPEN**: target customer may move from Excel-first SME to MSSQL/MySQL shops. Sections 1, 2-external and 3 are drafted-but-**not-amended** pending founder. Path (a) = existing EPIC-020 re-prioritised; path (b) = reversal of F27, not sliced. F37 medallion visibility -> proposed **EPIC-024**, founder to approve. F38 dms#43 retro-route, EPIC-008 + EPIC-017 completeness); 2026-08-23 (**F39**: `/loop` `/goal` "best DBMS + strong ontology + Palantir/Databricks-class platform" + parallel ticket/verify/CI ops — **re-ask**, no new PaaS epic; ontology owner = EPIC-019 + landed `scripts/ontology.py` + gated EPIC-021; RUN NOW #57 then #59; F36/F27 NEEDS-YOU stand); 2026-08-23 (**F40-F48 engine-side accuracy intake** (DR-0003 proposed + seven verified DMS scripts): refused-badge P0 RUN NOW under EPIC-017 + Cortex EPIC-001 (F40); CI accuracy gate + promote-on-truth under EPIC-018 (F42/F46); value dictionary + pinned plans under EPIC-019 (F46); stream ingest + .bak lane = EPIC-020, blocked F36 (F43/F44); **NEEDS-YOU**: accept DR-0003 items 1-2 and split EPIC-021 -> EPIC-021a grain-guarded compiler in the engine (F41), insights + brief thin epic vs EPIC-022 gate (F45); orphans #57 #59 #18 re-parented (F47); landing https://netie.ai/ breaches section 8 (F48)); 2026-08-24 (**F49-F69 founder platform ask + three re-verified P0 defects**: P0 A-0007 ungated Space-escaping preview reads -> **EPIC-003** (F51, READY); P0 A-0005 forged ledger actor on `/v1/pipelines/gold/sign` -> proposed **EPIC-025** (F50); no authentication anywhere -> **SCOPE decision, `DR-0004` proposed** (F49); ontology actions -> EPIC-005b/005a queued (F53); object-level row scoping -> EPIC-003, lattice declined (F55); compiled lineage -> **EPIC-024** (F67, F37 still awaiting founder); **declined**: apps over the ontology, AIP builder, PB scale, ML, notebooks, jobs, a table format, a catalog (F56-F64, F66); **NEEDS-YOU**: writeback is arguably a different product (F54), semantic versioning (F65), monetization with zero prior PRD coverage (F68); nearness measured - not nearer than F48 (F69))

---

## 1. Press release

### Ask your company's data a question and get an answer you can put in front of an auditor

**Netie DMS turns the spreadsheets and databases an SME already runs on into something
you can ask questions of - and every answer arrives with the rows it came from, the query
that produced it, and a record of who was allowed to see it.**

Malaysian logistics and distribution SMEs run on Excel. The numbers that go into a
monthly report are assembled by hand, and when someone asks "where did this figure come
from" three weeks later, the honest answer is usually that nobody knows.

General AI assistants make this worse rather than better. They will answer confidently
from a spreadsheet they have half-read, and there is no way to tell a correct answer from
a fluent one.

DMS is built the other way around. A **Space** is a sandbox over the sources a person is
actually allowed to see. Ask a question inside it and you get a number, the rows behind
it, and the SQL that ran. Ask something the data cannot support and it **says so** rather
than guessing. Every change to the underlying records goes through an action that lands
in a tamper-evident ledger.

> "I stopped keeping a parallel spreadsheet to check the system's numbers, because I can
> click any figure and see the rows." - *warehouse operations lead, pilot customer*

Available as a hosted pilot or self-hosted on your own infrastructure. Your data does not
leave your machine unless you say it can.

---

## 2. FAQ

### External

**How is this different from asking ChatGPT to read my spreadsheet?**
Three things. Access is enforced in the data plane, not by asking a model nicely - a
question that reaches outside your Space is refused, not answered. Every answer carries
its rows and its SQL. And the system is measured on how often it is *confidently wrong*,
with the target at zero; it abstains rather than guesses.

**What happens when it does not know?**
It abstains and says what it would need. This is the feature, not a limitation. A system
that always answers cannot be trusted on the answers that matter.

**Does my data leave my machine?**
Only if you allow it. Model routing and the leave-machine decision are a separate gate
you control.

**Can it change my data, or only read it?**
Both, but writes go through a proposal you confirm, and the confirmation lands in the
ledger with what changed and who approved it.

**What does it run on?**
Excel and CSV today, Postgres for the control plane. Your existing files, not a migration.

### Internal - the questions we cannot answer yet

**Q: Is our answer quality good enough to sell?**
Not proven. `wrong=0` on a 376-item corpus, but only **47 items are human-verified**, so
under the rule of three the true error rate is bounded at **6.4 percent**, not under 1
percent. The corpus also caught **none** of the last five confidently-wrong defects found
in live use. We do not have a trustworthy quality number, and the first thing this PRD
buys is one.

**Q: Does the Space boundary actually hold?**
No. `live_ask` mints its manifest from `demo_acl()`, which allowlists every table
regardless of `space_id`. The correct functions exist, are unit-tested, and have no
production caller. **Two customers in one room is a demo we cannot currently give.**

**Q: Can the system write?**
Barely. The action registry has 25 entries and **one** is invocable - it exports a
PowerPoint. Amend proposes and confirms but changes no warehouse data. The USP chain
"Ask -> Clarify -> Answer -> Amend -> ledger" is missing its last two links.

**Q: Is the generated-SQL path ready?**
No, and this is the strategic risk. Answers today come from a hand-written keyword
cascade (~240 lines of regex). The intended replacement measured **17 confidently wrong
against a floor of zero** and is switched off. We cannot honestly sell either state, and
we cannot resolve it without a real user's questions - our own paraphrases are
self-confirming.

**Q: How close is the Palantir-style ontology?**
Closer than the marketing claim, further than the ambition. Real object types, link
types, action types with read/propose/apply classes, and a function registry exist and
are load-bearing. What is missing is lineage and the write path. **We do not say
"Palantir" externally** until there is a paying client and the F-gates are hardened -
that stays parked.

**Q: What would make us abandon this?**
No user in four weeks. The failure mode here is not capability, it is eight workstreams
at 80 percent and nothing finished.

---

## 3. Out of scope

- Palantir/AIP marketing parity, lineage, full column-level provenance (`H6`, parked);
  includes Build-with-AIP examples gallery and cloning Workshop / Contour / Quiver /
  Cipher / OSDK / Agent Studio / Pipeline Builder / writeback / media backends
  (`P-DMS-30`, F29) unless founder amends H6; also Palantir SuperRepo / Foundry CLI
  local Ontology-as-code preview / Marketplace signed-bundle deploy (`P-DMS-31`, F30)
  — do not clone this sprint
- Google Sheets, or any source that is not a local file, Postgres, or - **amended F26
  (2026-08-05)** - a read-only MSSQL / MySQL extract via `sql_source` into bronze.
  **Live federation stays out of scope**: extract-to-lake only, so `duckdb.execute` stays
  inside `packages/executor` (hard rule 7) and every answered number keeps row-level
  provenance. True CDC / streaming stays out; a scheduled incremental extract with a
  **visible freshness watermark** is the supported answer to "my numbers are stale".
- CRAG / BIRD external benchmarks (parked behind Spaces)
- WASM or microVM isolation (host `tool_runner` is the path)
- The messaging/Closer vertical
- Multi-tenant hosting - self-host and single-tenant pilot only

---

## 4. Success assertion

> **WHEN** a person who is not the founder starts the stack from documented steps, opens
> a Space scoped to a subset of sources, and asks a question whose answer requires a
> table outside that Space, **THE SYSTEM SHALL** return an abstention naming the refusal
> - and when the question is inside the Space, return the answer, the contributing rows,
> and a drillthrough token that reproduces them.
>
> **AND WHEN** that person, in the same chat session, asks a grounded follow-up over the
> prior certified answer (e.g. "average of them", "add 2000"), **THE SYSTEM SHALL** either
> return a new certified answer computed from that prior result and the same granted
> sources, or abstain naming what it cannot do - never invent a number and never silently
> fall back to demo data.

Measured on the DMS envelope from `POST /v1/chat/ask`, not on Cortex-side state.

Product position (founder, 2026-08-03): the buyer wants **ChatGPT-for-Excel /
Genie-like usefulness over their own tables**, not anaphora-only chat. Hard laws still
bind: 0 confidently wrong, abstain-over-invent, `compliance_gate` on mutations. "Loosen"
means more capable grounded follow-ups and NL over granted tables - not silent demo
fallback and not inventing. Azure AI Foundry is a competitor/positioning mention, not a
build target.

---

## 5. Epic decomposition

Ordered by **irreversibility**, not value. Two in flight at a time, at least one visible.

### Wave 1 - trust the instruments, then hold the boundary

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-001** Eval gate can fail | Cortex | none | - | no |
| **EPIC-003** Space boundary holds | DMS | none | **resolved: store=B** | **yes** |

`EPIC-001` first because every number downstream is currently unfalsifiable. `EPIC-003`
paired with it so the wave produces something you can open and react to.

**EPIC-003 Space store (founder 2026-08-03): choice B.** Keep in-memory Spaces for demo
speed. Never imply persistence. One clean honest demo path (banner / truthful UX). Do
**not** switch the demo default to compose Postgres. Postgres remains the durable path
when explicitly configured; demo day runs memory + tells the truth about it.

### Wave 2 - close the ungoverned paths

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-002** Contract identity + wheel | Cortex | additive | - | no |
| **EPIC-004** Manifest enforced on `/dms/query` | Cortex | none | EPIC-001 | partially |

`EPIC-002` is split per the contract-stability rule: the identity fix ships alone before
any packaging, because publishing a wheel over a dual module identity turns a latent
divergence into a live one inside `canonical_manifest_bytes`.

### Wave 3 - make it write

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-005a** `amend.apply` action type + tool_runner branch | Cortex | **additive** | EPIC-002 | no |
| **EPIC-005b** Confirm invokes `call_action`, receipt shows the diff | DMS | none | EPIC-005a | **yes** |

Three-epic contract pattern collapsed to two because the change is additive: the engine
gains an action, the contract gains an endpoint, DMS adopts it. `EPIC-005b` cannot ship
against the published contract, so it is genuinely blocked - not merely sequenced.

### Wave 4 - one surface, then the asset (EPIC-008 pulled forward)

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-007** One UI, not two | DMS | none | EPIC-005b | **yes** |
| **EPIC-008** Two-minute demo asset | DMS | none | EPIC-003 store=B (not EPIC-005b) | **yes** |

`EPIC-007` deletes `demo/dms-ui` in Cortex. Two UIs for one product is a maintenance tax
already causing stale cross-references.

**Founder override 2026-08-03 (F7):** pull **EPIC-008** forward now - preview pagination,
`_src` render, clean demo surface, stranger path - without waiting on amend/write path.
Thinner demo-honesty epic; write-path polish stays behind EPIC-005b.

### Wave 1b - multi-turn grounded follow-ups (founder amendment F4)

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-011** Multi-turn grounded follow-ups | Cortex | none (additive if session wire moves) | DMS chat already passes `session_id` | **yes** |

Buyer sees query jukebox, not analyst. Follow-ups over prior certified answers must
compute from that prior result or abstain. Hard laws unchanged: no invent, no silent
demo fallback. Pairs with EPIC-008 stranger path for end-to-end demo proof.

### Wave 5 - governed freeform, then grounded presentation (founder F10–F18)

**F17 override (2026-08-03):** product path is DMS UI/API -> Cortex ask with L2 on +
Space-scoped RAG after SQL abstain + insights -- **not** AirGPT-only. EPIC-011 engine
tickets (Cortex#20-22) are closed; dms#16 is residual stranger verify (parallel lane),
**not** a hard gate on EPIC-015 RAG-01. Keep RAG-03 sequenced after INS close on
`answer_engine.py`.

**F18 overnight WIP (2026-08-03):** **EPIC-003** Space-only UI (visible BOUNDARY) +
**EPIC-015** RAG-01 foundation. Finish **EPIC-013** via dms#17 in the same wave (Cortex
INS already closed). DMS bench vs AirGPT gold is EPIC-015 acceptance (read-only reuse of
`D:\AirGPT\tests\RAG\*` — no re-download). Do **not** reopen EPIC-012 wholesale; L2
warehouse parity tickets go under EPIC-015 / Cortex regression, sequenced on
`answer_engine.py` **before** RAG-03. AirGPT EPIC-D01 stays separate Cortex-direct demo.

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-012** Basic L2 FreeRoute (governed freeform SQL) | Cortex (+ DMS flag/env) | none | OpenVault FreeRoute reachable; sql_validate_gate stays closed | **yes** -- **CLOSED** (winner `auto`) |
| **EPIC-013** Insights + chart_spec secondary pass | Cortex (+ DMS surface) | none if bullets ride `answer`/`assumptions`; **additive** if new `insights[]` wire | rows exist on any L0/L1/L2 path; **close via dms#17** (F18) | **yes** |
| **EPIC-014** MCP server (ask / preview / list_metrics) | DMS | none (or additive tools schema later) | EPIC-008 stranger path usable; not demo-critical | no |
| **EPIC-015** Space-scoped governed RAG (hybrid retrieve + manifest) | Cortex (+ DMS ingest/index) | **additive** (`sources[]`, scoped chunk index) | EPIC-003 ACL closed; Space-UI residual open; RAG-03 waits on INS+#17 + L2-WH before `answer_engine`; AirGPT pattern+gold only | **yes** -- **RUN NOW** (F15–F18) |

**Demo / live ask defaults (F17 -- no PRD amendment):** L2 on via
`Start-DMSStack.ps1 -EnableL2` (`DMS_L2_MODEL=auto`). Doc-RAG SiRA **depth=max** is the
DMS live default when EPIC-015 retrieve lands (tighten RAG-02 acceptance). Still FreeRoute
`auto` -- not a permanent pin to paid frontier models (that would be a separate amendment).

**Code check (2026-08-03):** L2 path exists; EPIC-012 closed. `rag_answer` remains a
contracts-dir stub (`query_service.py` ~1048). EPIC-013 INS-01/02 self-verified, open for
independent close. No MCP server in DMS.

**EPIC-012 vs EPIC-006:** EPIC-012 = Basic L2 on (top-N / out-of-certified via validated
FreeRoute SQL, verifier loop, model rotate on gate failure, abstain-over-invent).
EPIC-006 remains full keyword-cascade *replacement* and stays deferred on held-out
corpus. Do not merge them.

**Same-file note:** EPIC-013 INS-* and EPIC-015 RAG-03 both touch
`CortexOS/dms/answer_engine.py`. RAG-01 (schema) and RAG-02 (retrieve) do **not**.
Sequence: finish INS independent close -> then RAG-03. Do not dual-WIP the file.

### Deferred, with the condition stated

| Epic | Why not now |
|---|---|
| **EPIC-006** C7 - replace the keyword cascade | **Founder-override ticketed** (Cortex#17) but still needs a held-out real-user set OR an adversarial corpus not written by the team. Not the same as EPIC-011 or EPIC-012 (Basic L2 enablement). |
| **EPIC-009** Column lineage (`C9-full`) | Ontology depth. After a paying client. |
| **EPIC-010** `claim_n` 47 -> 310 | Founder-attended, TTY-gated (Cortex#18). Schedule it, do not assign an agent alone. |
| **EPIC-014** MCP server | Surface for later; unlock when stranger demo path is green and buyer asks for IDE/tooling integration. |

### Wave 6 - Excel-native Copilot orchestration (AUTHORIZED to file; build later)

**Status:** founder via AirGPT F15 (2026-08-03): PRD→epic→tickets now; **NEEDS-YOU resolved 2026-08-03** (Pointer-primary + Copilot dependency YES); **implementation deferred**. Cross-ack AirGPT Wave Demo-2 / EPIC-D04 (identify+prompt+reveal only — AirGPT must not become a second orchestrator). Pointer owns Copilot panel paste.

**Pipeline (authoritative):** AirGPT RAG identify+draft insightful pack → **DMS governed cross-check/strengthen/validate** (prompt quality + schema/SQL sanity) → **Pointer** paste into Excel Copilot on correct workbook → DMS extract → AirGPT reveal. `user-excel` MCP = secondary/optional only. AirGPT F14/#21 SQL parallel.

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-016** Prompt-pack cross-check → Pointer→Excel Copilot → extract resulting workbook | DMS (+ Pointer paste; Cortex action gate if ledgered writes) | none or additive | Space path usable; AirGPT EPIC-D04 prompt pack; Pointer paste lane | **yes** |

**Repo:** Netie-AI/dms (capability) / Pointer (paste) / Cortex only if exports become governed actions
**Contract impact:** none preferred; additive if receipt/export artifact wire needed
**Depends on:** not blocked on EPIC-013 chart_spec or EPIC-014 ask/preview MCP; paste path = Pointer (not MCP-primary)
**Irreversibility:** CAPABILITY (cross-excel orchestration) + BOUNDARY (DMS cross-check; Pointer paste; Copilot dependency) + DEMO
**Appetite:** tickets filed (#29-32); epic-agent **amend bodies**; build after current WIP or founder override

**Acceptance (customer seat):**
> **WHEN** handed a structured prompt pack for `frtr_00027_supply-chain-regional.xlsx` asking OnTime=true average cost + separate export sheet + chart for PPT, **THE SYSTEM SHALL** governed-cross-check/strengthen the pack, have Pointer paste the validated prompt into Excel Copilot on the correct workbook, and return a resulting workbook with Cover / OnTime Export / Analysis / Presentation Chart and avg on-time cost in the Excel Copilot ballpark (~USD 300 across ~184k OnTime rows).

#### EPIC-016 - ticket batch (filed #29-32; epic-agent amend — do not implement yet)

1. **XLSX-ORCH-10** [#30](https://github.com/Netie-AI/dms/issues/30) Governed cross-check + Pointer→Excel Copilot multi-step - WHEN handed AirGPT candidate prompt pack + frtr_00027 path, DMS SHALL cross-check/strengthen/validate (incl. schema/SQL sanity), then hand validated prompt to **Pointer** for Excel Copilot panel paste on the correct workbook (MCP secondary/fallback only), producing Cover / OnTime Export / Analysis / Presentation Chart.
2. **XLSX-ORCH-11** [#31](https://github.com/Netie-AI/dms/issues/31) Extract whole workbook - WHEN the run completes, DMS SHALL store/return the resulting xlsx in Space Docs for audit and AirGPT reveal.
3. **XLSX-ORCH-12** [#32](https://github.com/Netie-AI/dms/issues/32) FRTR golden pack - WHEN the acceptance Q runs, numbers match Copilot ballpark with sheet provenance — not AirGPT RAG Summary theater.

**Founder decisions (F25/F15 NEEDS-YOU closed):** (1) **Pointer-primary** Copilot panel paste; (2) **Excel Copilot IS accepted path dependency (YES).** DMS owns prompt-pack quality cross-check before Pointer paste.

**Not EPIC-013** (chart_spec after SQL rows). **Not EPIC-014 reopen** (ask/preview/list_metrics). **Not AirGPT #21** (in-process SQL RAG). **Not** MCP as primary Copilot paste.

### Wave 7 - the number boundary, then free-form coverage (founder F26)

**Origin:** measured demo failure 2026-08-05 (see F26). The failure was **not** a merge
across files and **not** a scope error. An LLM summed numbers it read in retrieved text.
Scoping alone would not have caught it - a precisely cited wrong number is worse than an
uncited one, because the citation manufactures confidence.

#### The metric change

"Accuracy" is retired as a single number. It is the metric that let AirGPT score top on
primary50 and still lie to a client. Two metrics replace it:

| Metric | Definition | Target |
|---|---|---|
| **Precision on answered** | of answers rendered with a confident badge, the share matching a regenerated oracle on **both magnitude and rank** | **100.00%. Zero confidently wrong. This is a law, not a goal.** |
| **Coverage** | share of the question set answered rather than abstained or sent to clarify | grows every wave; **never bought with precision** |

Free-form is delivered by raising coverage under a fixed precision law. This is how the
comparators actually produce their published numbers. Snowflake Cortex Analyst runs a
classification agent that **rejects vague questions up front** and offers similar answerable
ones, then reports 90%+ on what it does answer. Databricks' own production-Genie walkthrough
reaches 100% on **13 curated questions** after four curation passes, and sets **>80% as the
bar before UAT**. Nobody ships 99.99% open-domain: the best agent frameworks solve **21.3%**
of Spider 2.0, the enterprise-schema benchmark, against 91.2% on the toy Spider 1.0.

**99.99% is a precision target. It is never a coverage target.** Stated the other way: we
will answer fewer questions than a chatbot, and every one we answer will survive an auditor.

#### No regex

The refusal boundary is hardcoded. The question is not. L2 writes SQL freely, DuckDB
executes it, a verifier re-derives it. Generality comes from the model; safety comes from
execution and verification. No intent regex, no keyword cascade (that is EPIC-006, still
deferred and still separate).

#### The answer ladder - L3 bar is the client fix

| Tier | Path | May emit a number? |
|---|---|---|
| L0 / L1 | certified / verified query | yes |
| L2 | free-form generated SQL, validated, executed, verified | yes |
| **L3** | **doc-RAG over unstructured text (EPIC-015)** | **no. Prose and citations only. An aggregate question hands off to L2 or abstains.** |

L3 is exactly where AirGPT failed. The bar is a **type boundary**, not a filter or a
heuristic: a path with no executed rows has no authority to render a number.

#### What already exists - do not rebuild

Five of the six mechanisms Cortex Analyst uses for its 90%+ are already in the estate:

| Cortex Analyst mechanism | Already here | Gap |
|---|---|---|
| Semantic model | `D:\Cortex\packs\dms\ontology\{object,link,action}_types.yaml`, `functions.yaml` | demo-shaped, not customer-shaped |
| Verified query repository | L0/L1 certified path | not curatable by a steward -> EPIC-019 |
| Literal / value semantic search | hard rule 12 (value normalization) is already law | not wired into L2 generation |
| SQL-compiler error-correction loop | EPIC-012 `sql_validate_gate` + verifier loop (CLOSED) | not applied to the L3 path |
| Multi-model rotation | OpenVault FreeRoute `auto`, rotate on gate failure | none |
| Ambiguity classification, refuse up front | abstain-over-invent law | no clarify-and-offer-similar-questions UX |

**The product gap is curation UX and the L3 bar, not engine capability.**

| Epic | Repo | Contract | Depends on | Visible |
|---|---|---|---|---|
| **EPIC-017** Numeric answer boundary (envelope E9) | DMS (+ Cortex if badge wire moves) | none | envelope validator exists | **yes** - **RUN NOW** |
| **EPIC-018** Coverage/precision instrument | DMS | none | EPIC-017 | **yes** - **RUN NOW** |
| **EPIC-019** Verified query repository (trusted assets) | Cortex ontology pack + DMS Studio | additive | EPIC-018 measures it | **yes** - next |
| **EPIC-020** `sql_source` connector (MSSQL + MySQL -> bronze) | DMS | none | OOS amendment (section 3) | **yes** - next |

**EPIC-017 acceptance:**
> **WHEN** any answer path renders a value the reader will treat as a number, **THE SYSTEM
> SHALL** carry a provenance pointer to executed rows for that value; and **WHEN** no such
> pointer exists (doc-RAG prose, model-composed total), **SHALL** abstain or drop to a
> non-confident badge naming why. Asserted as **E9** on the `POST /v1/chat/ask` envelope
> alongside E1-E8 - not Cortex-side.

**EPIC-018 acceptance:**
> **WHEN** the eval suite runs, **THE SYSTEM SHALL** report precision-on-answered and
> coverage as two separate numbers against an oracle **recomputed at test time** by a path
> independent of the answer path, asserting **both magnitude and rank order**, and **SHALL**
> fail the run on a single confidently-wrong answer regardless of coverage.

**Fixture defect - blocks EPIC-018 scoring.** The corpus at `D:\AirGPT\data\rag\16\Docs\`
is degenerate: all four "different" workbooks (`sales_messy`, `ops_messy`,
`inventory_messy`, `mixed_messy`) carry the **same seven sheet names** - Sales, Inventory,
Messy_RAW, Ops_KPI, Returns, Summary, Wide_Fill - with near-identical shapes. A corpus of
clones cannot distinguish scope-correct from scope-blind, which is why primary50 scored
AirGPT top. `Wide_Fill` totals are ~2x `Sales` totals in the same book, so "which sheet"
changes the answer by 100%. Regenerate with genuinely distinct domains before scoring
coverage. AdventureWorks (`.bak` files already at DMS repo root) is the referential-integrity
oracle; it is a **test asset, not a runtime dependency**.

**EPIC-019 (trusted assets)** is the coverage engine. Databricks report that adding example
queries "delivered more value than any other single iteration step". A steward marks an
answer certified in Studio; it becomes a retrievable verified query. Coverage rises by
curation, not by loosening the badge.

**EPIC-020 (`sql_source`)** is **one** connector with a driver enum, not two. MSSQL and MySQL
differ by driver string. Uses the existing catalog + secrets ports - no sixth abstraction
(hard rule 6). Extract-to-bronze, not live federation, so provenance and hard rule 7 hold.
Space grants apply at table level.

**Model providers.** DeepSeek is a **model-provider port swap**, already one of the five
ports - a config value, not an epic. FreeRoute stays `auto`. **NEEDS-YOU:** the L2 generator
sees schema and sample literals, so a China-hosted API is a PDPA / data-residency decision
for Malaysian buyers. Either self-host or surface an explicit per-Space consent flag. Do not
default customer data to it silently (hard rule 11 class).

#### Deferred in this wave, with the condition stated

| Epic | Why not now |
|---|---|
| **EPIC-021** Customer-shaped semantic / metric layer | Unlocks when EPIC-020 has landed one real customer schema. Snowflake's own BIRD test shows a semantic model lifting the same LLM 57% -> 78%, so this is the largest single coverage lever - but it is worthless before there is a real schema to model. |
| **EPIC-022** Generative app authoring over the ontology | **Gated on precision, not on time.** Unlocks when EPIC-018 reports 100.00% precision on answered across two consecutive waves with coverage >= 60%. Generating apps over an unverified metric layer mass-produces confidently wrong dashboards. |
| **EPIC-023** What's New + guided tour (existing surfaces only) | **SURFACE, UI-only.** Origin F29. Founder confirmed **A** (thin slice). Unlocks when Wave 7 has a free WIP slot (at least one of EPIC-017/018 closed or parked). **Approved but not RUN NOW** — accuracy first. Style when built: pretty, curvy, minimalist, interactive; polish only, no fake backends. Does **not** unlock H6 / Build-with-AIP gallery (B rejected; P-DMS-30 stays parked). No tickets until unlock. |

#### Positioning - unchanged, deliberately

The internal goal (a self-serve Foundry/Genie for companies with MSSQL, MySQL and Excel who
cannot fund an FDE team or Azure AI Foundry pricing) is **recorded and endorsed**. The
external claim stays parked: `H6` in section 3 and the section 8 rule both stand - nothing
in the press release appears in an external asset until it traces to a passing gate.
"Replaces Palantir/Databricks" traces to EPIC-018 reporting precision, and to nothing else.

**Cortex stays the only orchestrator.** DAG / loop / workflow selection lives in the engine.
DMS gains a free-form *surface*, never a second orchestrator - same rule already applied to
AirGPT in F25.

**F27 closed 2026-08-05:** founder YES on both declines (Cassandra/Scylla; MCP / plugins /
ontology inside customer MSSQL). EPIC-020 stays extract-only. Accuracy path is Wave 7 +
Cortex orchestrator depth — not a warehouse brand swap (see F28, P-DMS-28/29).

**F29 (2026-08-05) — surface vs H6 split (NEEDS-YOU closed afternoon = A):**
- **In product (EPIC-023, deferred SURFACE):** after each build, a dismissible
  What's New sticker / smooth popup sourced from shipped changelog; button-to-button
  guided tour over **existing** DMS pages only (Spaces, Chat, Studio, Library, Ontology;
  Honesty: E9 / abstain / Space scope). Same engine reusable as section tutorials. UI-only
  — no fake backends, no invented screens. Style constraint: pretty, curvy, minimalist,
  interactive. Approved thin slice; **do not run / do not file tickets** until Wave 7 WIP frees.
- **Out / parked under H6 (`P-DMS-30`) — B rejected:** cloning Build-with-AIP examples
  catalog or Workshop / Contour / Quiver / Cipher / OSDK / Agent Studio / Pipeline Builder /
  writeback / media **backends**. No H6 amend. Ontology stays Cortex-authored; DMS renders.
  EPIC-022 remains the only generative-app path and stays precision-gated.

**F30 (2026-08-05) — SuperRepo is dual-brain, not a tour:**
- Palantir SuperRepo beta (pro-code monorepo: Ontology + functions + React as one
  versioned artifact; Foundry CLI preview; Marketplace signed bundle) maps to
  **EPIC-022** (precision-gated) + Cortex **P14** dual-brain / ENGINE_SDK + **P17**
  engine packaging + packs ontology YAML + O4 Agent SDK — **not** EPIC-023.
- Internal goal ("SuperRepo-class path for MSSQL/MySQL + Excel shops") stays under
  the existing positioning endorsement above; external claim stays **H6**.
- **Do not** clone Foundry CLI / Marketplace / SuperRepo this sprint. Park
  `P-DMS-31`. F29 NEEDS-YOU closed **A** (see F29 disposition).

**F31 (2026-08-05) — adaptive architecture chooser is Cortex research, not Wave 7:**
- Founder wants the answer orchestrator to choose Gen / C / JEPA / FSM / DAG vs
  linear verify loop. That is **Cortex plane 3** (already gen-cFSM G1.0/G1.1;
  enterprise loop parked **P21**). It is **not** a DMS epic and does **not**
  replace EPIC-017/018 RUN NOW.
- Ask-path default stays **linear FreeRoute verify loop** (F28). DAG / meta-chooser
  only after EPIC-018 shows a measured linear-refine bottleneck **or** founder
  names an ask-path policy switch. Honesty: never claim trained JEPA shipped.
- Must not bypass envelope E1–E9, abstain-over-invent, or five ports (no sixth).
- Test **now** via `score_answers` + envelope; land hostile eval pack under
  EPIC-018 (use cases designed to fail green). Park ask-path selector **P-DMS-32**.

---

## 6. Cross-product acknowledgement

Epics in this PRD that land in Cortex are engine work that other consumers inherit:

```
Serves: PRD-001 (DMS) EPIC-001, EPIC-002, EPIC-004, EPIC-005a, EPIC-011, EPIC-012, EPIC-013, EPIC-015
Serves: PRD-001 (AirGPT) F15 / EPIC-D04 — EPIC-016 owns governed prompt cross-check + Excel path; Pointer owns Copilot panel paste; AirGPT surfaces only
Serves: PRD-001 (Cortex) EPIC-001 (Netie-AI/Cortex#11) - F40 refused-badge engine half (`_is_abstain_signal` + unknown-badge default); DMS EPIC-017 (dms#33) asserts it on the envelope
```

That line belongs in the Cortex PRD when it is written. AirGPT and Pointer both consume
the same manifest enforcement from `EPIC-004`, so it is engine work, not DMS work that
happens to live in the engine.

---

## 7. Feedback ledger

Append only. This table is the PRD's memory - it is how feedback given weeks ago lands in
the right epic instead of becoming a duplicate.

| # | Date | Raised in | Feedback | Routed to | Outcome |
|---|------|-----------|----------|-----------|---------|
| F1 | 2026-08-03 | founder override | C7 (EPIC-006) must be ticketed and scheduled into an immediate wave despite prior deferral; paraphrases alone still cannot settle it, so acceptance must require a held-out real-user question set OR an adversarial corpus not written by the team. | EPIC-006 / Cortex#17 | ticketed; supersedes Cortex#12 |
| F2 | 2026-08-03 | founder override | claim_n 47->310 (EPIC-010) must be scheduled as a founder-attended ticket (TTY-gated), not left as "not an epic". | EPIC-010 / Cortex#18 | ticketed; supersedes Cortex#13 |
| F3 | 2026-08-03 | founder override | DMS CORTEX_CONTRACT_TOKEN now set at repo level because org secrets cannot reach private repos on current plan; checkout of Cortex for wheel build is required until Cortex#5 dual-module identity / published wheel is fixed. | EPIC-002 / Cortex#16 | DMS secret set; dms#3 closed; dms#7 mypy remains |
| F4 | 2026-08-03 | live demo + biz-tech-translator (blocking) | Follow-ups over prior certified answers must work ("average of them", "+ 2000"); keep prior-turn result context; arithmetic/simple transforms only when grounded; else abstain. Buyer sees query jukebox not analyst. | EPIC-011 | **Founder YES** - PRD success assertion amended for multi-turn; EPIC-011 opened; FOLLOWUP tickets filed. |
| F5 | 2026-08-03 | live demo + biz-tech-translator (blocking) | Uploaded files + Spaces must match buyer view and survive demo: in-memory banner / dies on restart; Studio fixture bronze + old paths; new uploads hard to find; Company scope shows gl_export.csv confusingly. Need durable demo Spaces OR one honest durable path; Studio lists uploads for active Space; hide fixture bronze from demo tenant view. | EPIC-003 / dms#6 | **Founder store=B** - keep in-memory for demo speed; never imply persistence; one honest demo path. Do not switch demo to compose Postgres as default. Tickets: DEMO-SPACE-01, STUDIO-01, STUDIO-02; P-DMS-26 stays on EPIC-003. |
| F6 | 2026-08-03 | live demo + biz-tech-translator (blocking) | Full table view + navigation: preview 8-10 rows only; `_src` renders as [object Object]; `/v1/library/bronze/.../preview?limit=50` 404 for tables in tree. Need preview 200 for valid ids; UI pagination + total row count; fix object render. | EPIC-008 | **Founder YES pull-forward.** EPIC-008 opened; PREVIEW-01/02/03 tickets filed. |
| F7 | 2026-08-03 | live demo + biz-tech-translator (blocking) | One clean demo path: stranger upload -> ask -> follow-up -> audit/scope chip matching file in <10 min; remove Company-scope noise and fixture bronze; extend `verify_demo_live` with follow-ups. | EPIC-008 | **Founder YES pull-forward** (thinner demo-honesty epic; not waiting on EPIC-005b). DEMO-PATH-01 filed. Follow-up half gated on EPIC-011. |
| F8 | 2026-08-03 | STATUS vs GitHub reconcile | STATUS.md claims dms#4 P0-DEMO-01, dms#2 ACL-01, dms#5 P0-DEMO-03 closed this session; GitHub still OPEN. Commits exist (`6c22817` ingest, `01424da` acl, `174f0f9` grounding). | epic-agent verify+close | **Verified 2026-08-03:** `pytest tests/test_ingest_receipt_truth.py tests/test_grounding_manifest.py tests/test_space_acl_boundary.py tests/test_space_boundary_envelope.py` -> **23 passed**. Issues closed. EPIC-003 remains open (P-DMS-26 + honesty/Studio tickets). |
| F9 | 2026-08-03 | founder + client mention | Product is ChatGPT-for-Excel / Genie-like usefulness, not anaphora-only; willing to loosen security somewhat toward capable grounded NL. Client mentions Azure AI Foundry for warehouse data. | EPIC-011 + positioning | Routed: usefulness = EPIC-011 (+ later EPIC-006). Loosen != invent / silent fallback / skip compliance_gate. Foundry = competitor note only, not a build. |
| F10 | 2026-08-03 | founder ask (PRD Agent) | Basic L2 FreeRoute: enable governed freeform SQL (`DMS_L2_ENABLED`), compare vs L1/certified, verifier loop, OpenVault model rotate if wrong; freeform still passes SQL validate; abstain-over-invent; top-N / out-of-certified via L2. Related C7-full. Demo: #16 needs Cortex#19 follow-ups first. | EPIC-012 (new) | **Sliced.** Not EPIC-006 full cascade replace. Code already has L2 path flag-off. Epic-agent to open Cortex epic + tickets. Tomorrow WIP: stranger demo before L2 spike. |
| F11 | 2026-08-03 | founder ask (PRD Agent) | Insights + chart_spec secondary pass AFTER rows: grounded bullets + chart type from result schema; no numbers not in rows. Separate from L2 SQL path. Deterministic CSV export polish = thin UX ticket (no AI). | EPIC-013 (new) | **Sliced.** Sequence after EPIC-012 on `answer_engine.py`. Prefer no new wire field (ride answer/assumptions + existing chart_spec); additive contract only if structured `insights[]` required. |
| F12 | 2026-08-03 | founder ask (PRD Agent) | MCP server later surface: ask / preview / list_metrics; not core demo. | EPIC-014 (new) | **Sliced + deferred.** Unlock after stranger demo path; lower priority than EPIC-012/013. |
| F13 | 2026-08-03 | founder ask (PRD Agent) | Deterministic CSV export polish under insights/UX if needed (no AI). | EPIC-013 thin ticket | Client CSV download already in `AnswerMessage.tsx`; polish only if broken/incomplete under demo. |
| F14 | 2026-08-03 | founder demo-eve ask | One last L2 FreeRoute model bakeoff (`verify_l2_vs_l1.py` across free tiers); promote winner to `DMS_L2_MODEL` / `Start-DMSStack.ps1 -L2Model`; then formally close EPIC-012 (Cortex#23-28) and unblock EPIC-013 (Cortex#29-31, dms#17-18). RAG strength + hybrid_max 6/6 is **AirGPT EPIC-D01** (Cortex-direct), not OpenVault FreeRoute — do not merge into DMS L2 epic. | EPIC-012 closure + EPIC-013 gate | **Routed.** L2 bakeoff = Cortex L2-03 + one verify run; close 012 only after adversarial verify. 013 blocked until 012 closed (`answer_engine.py` same-file rule). EPIC-012 **CLOSED** 2026-08-03; EPIC-013 unblocked. |
| F15 | 2026-08-03 | founder ask (PRD Agent) | Learn from / copy AirGPT RAG architecture (`D:\AirGPT` `hybrid_max` 6/6 on `frtr_0001`) into DMS Spaces: select Space, add files, only see that Space's sources. Improve ask performance on uploaded workbooks/docs. | **EPIC-015** (new) | **Sliced — founder YES to open epic + tickets.** Not EPIC-013 (presentation after rows). Not EPIC-012/L2 (governed SQL). Not fold into AirGPT EPIC-D01 (stays Cortex-direct demo). Copy **pattern**: space-scoped hybrid retrieve (BM25+vector+SiRA depth), `source_ids` narrow, route order L0/L1/L2 then doc-RAG. **Do not copy**: Cortex-direct ungated LLM, pandas `frtr_sql_baseline` lane, skip manifest/compliance_gate, Python post-filter scope, openpyxl write. DMS `rag_answer` today is stub (`query_service.py:926` contracts dir only). |
| F16 | 2026-08-03 | founder ask (PRD Agent) | Insights generation is the most important product reason — prioritize EPIC-013 output after rows/RAG grounding; not a side feature. | EPIC-013 | **Founder YES implicit** (re-prioritize run order). RUN NOW: Cortex#30-31 then dms#17 in parallel with EPIC-011 close-out. Insights pass runs on any path that returns rows (L0/L1/L2 today; EPIC-015 doc path later). |
| F17 | 2026-08-03 | founder ask (PRD Agent) | Direct interact from DMS (not AirGPT-only): Space-scoped RAG + L2 FreeRoute (`auto`) + Cortex max effort on live ask; path = DMS UI/API → Cortex ask (L2 on) → RAG when SQL abstains → Space files only in that Space → insights. Confirm RUN NOW order; whether SiRA depth=max / L2-always-on needs PRD amendment; unblock RAG-01..05 or wait on dms#16. | EPIC-015 + EPIC-013 + EPIC-012 (closed ops) + EPIC-008 (#16 residual) | **Routed — no PRD amendment** for SiRA depth=max or L2-on-demo (ops defaults inside EPIC-015 RAG-02 + existing `-EnableL2`/`DMS_L2_MODEL=auto`). **NEEDS-YOU** only if "best models" means pin paid frontier forever (leave FreeRoute `auto`). **Unblock RAG-01 now** (EPIC-003 ACL tickets closed; schema != `answer_engine`). Keep RAG-03 blocked until INS-01/02 independent close. Do **not** wait on #16 to start RAG-01; #16 = parallel verifier lane. AirGPT EPIC-D01 stays separate demo track. |
| F18 | 2026-08-03 | founder overnight (PRD Agent) | (1) Benchmark DMS L2/RAG on AirGPT questions already on disk — `D:\AirGPT\tests\RAG\FRTR-bench` + `t2-ragbench` + gold/results memory; **do not re-download**. (2) Improve L2+RAG where <perfect; debug/retry to **warehouse-style 10/10 or higher**; expand FRTR (all books) and t2-ragbench sensibly in batches. (3) Every DMS Space UI window shows **only** active-Space sources (no Company/cross-Space junk; like separate repos). (4) Insights generate; finish incomplete (dms#17, RAG-01 #24, etc.). (5) No cross-source leakage. | EPIC-015 (bench+score+RAG) + EPIC-003 (Space-only UI) + EPIC-013 (dms#17) + EPIC-012 closed (L2 score chase = new tickets, not full reopen) | **Routed — no PRD amendment.** Eval corpus = existing AirGPT assets only (not CRAG/BIRD product scope). AirGPT warehouse 10/10 is Cortex-direct pandas-sql, **not** DMS path — DMS must clear same `warehouse_gold` via envelope. EPIC-003 STUDIO/DEMO tickets closed but Library/TopBar/Spaces still expose Company (`library.py` gl_export + TopBar Company option) — **decomposition miss**; epic-agent file SPACE-UI tickets. Unblock: epic-agent tickets for BENCH-DMS + SPACE-UI-ALL; runner queue below. WIP overnight: **EPIC-003** (visible) + **EPIC-015** (RAG-01); close EPIC-013 via #17 in same wave. |
| F19 | 2026-08-03 | founder (PRD Agent) | Demo docs already written: `D:\DMS\docs\DEMO_RUNBOOK.md` (dual DMS + AirGPT flow). Acknowledge; is it the dual-demo artifact? | EPIC-008 (closed residual docs) + EPIC-015 (Act C/D RAG half) + AirGPT `tests/RAG/DEMO_RAG.md` | **Ack — no new epic.** DEMO_RUNBOOK is **sufficient** as the dual-demo artifact for demo day (ports, Acts A–D, skip list, pre-flight). AirGPT detail stays in `DEMO_RAG.md`. Keep both; do not invent a third runbook. |
| F20 | 2026-08-03 | founder (PRD Agent) | Sync demo files so buyers open source → hover → Explorer on original file. AirGPT has `POST /api/rag/reveal-path`; DMS SourcePanel only shows `origin_uri` text (`SourcePanel.tsx`). | **EPIC-015** (SURFACE ticket REVEAL-01) + demo ops sync (runbook §5) | **Filed.** REVEAL-01 = dms#26. DEMO-SYNC-01 **skipped** (runbook §5 manual sync sufficient). No AirGPT HTTP bridge. |
| F21 | 2026-08-03 | founder (PRD Agent) | Connect DMS demo to AirGPT MAX RAG (`D:\AirGPT`): ingest separately like Spaces; freeform prompts; citations open originals; evaluate both on shared + edge cases. | EPIC-015 (shared gold/eval) + AirGPT EPIC-D01 / `DEMO_RAG.md` (MAX hybrid) + F19 runbook | **Filed optional.** DUAL-EVAL-01 = dms#27. Two products / two URLs; no codebase merge. |
| F22 | 2026-08-03 | founder readiness audit (PRD Agent) | STATUS claims RAG-04 #23 / RAG-05 #22 shipped; GitHub still OPEN. Cortex EPIC-015 #34 OPEN; RAG-01/02/03 CLOSED. | EPIC-015 / epic-agent verify+close | **Queued verify-close.** #23/#22 bodies+comments updated with verify commands; Cortex#34 noted. No re-implement. Ticket-runner: independent verify then close; update #34 checkboxes after. |
| F23 | 2026-08-03 | founder readiness audit (PRD Agent) | Postgres Spaces: EPIC-003 #6 — founder already chose memory for demo (choice B). Do not reopen as default Postgres. | EPIC-003 store=B (stands) | **Confirmed — do not reopen / do not amend.** Demo = memory + honest banner. Postgres remains optional durable path when explicitly configured. |
| F24 | 2026-08-03 | founder readiness audit (PRD Agent) | (a) Reorder/low-stock live asks: E4 envelope orphan-number crash — no ticket yet; skip in demo. (b) Stack stability (kill stale ports) — ops, not product unless productized. | (a) **EPIC-008 reopen** for ENV-E4 ticket only; (b) none — ops | **Filed.** EPIC-008 #8 reopened; ENV-E4 = dms#28 labeled `post-demo`. Do not build before demo. (b) ops only in DEMO_RUNBOOK — not productized. |
| F25 | 2026-08-03 | AirGPT PRD intake (founder via Cursor) | FRTR 00027 multi-part Q (OnTime avg + export xlsx + chart for PPT) fails on AirGPT RAG; Excel Copilot succeeds. Prefer identify workbook + structured prompts → open Excel → Copilot panel/MCP → extract workbook. Cross-excel orchestration most important; connect DMS; PRD→epic→tickets now, build later. | **EPIC-016** (new) + AirGPT EPIC-D04 | **Sliced — founder authorized file epic/tickets; leave build later.** DMS owns Excel drive/orchestration. AirGPT = identify + prompt pack + handoff only (OOS second orchestrator). **Not** EPIC-013 chart_spec, **not** EPIC-014 reopen, **not** AirGPT #21 SQL. Serves: AirGPT PRD-001 **F15**. Tickets filed: dms#29-32. **NEEDS-YOU RESOLVED 2026-08-03 founder:** **Pointer-primary** paste into Excel Copilot; **Copilot path dependency YES**; `user-excel` MCP secondary/optional only. **DMS owns** governed cross-check/strengthen/validate of AirGPT prompt pack (and schema/SQL sanity) **before** Pointer paste. Pipeline: AirGPT pack → DMS cross-check → Pointer→Copilot → DMS extract → AirGPT reveal. Epic-agent: amend #29-32 bodies; do not start runners yet. |
| F26 | 2026-08-05 | founder + client demo failure (**measured**, not reported) | Client asked "top 3 category sales" in AirGPT over the messy workbook Space. AirGPT returned Home 383,803.56 / Sports 242,755.97 / Misc 228,548.84, citing `aa64458a_p50_03_inventory_messy.xlsx :: Wide_Fill` and four files. Measured truth for that sheet: Misc 2,691,552.10 / Apparel 2,402,425.68 / Sports 2,358,800.10 - **wrong magnitudes and wrong ranking**; Apparel, the real #2, never appeared. Client's hand-filtered figures (Home 1,199,018.49 / Electronics 1,545,366.40 / Misc 380,948.33 in `p50_01_sales_messy :: Sales`; Electronics 1,563,234.12 in `p50_03_inventory_messy :: Sales`) reconcile to the cent. Brute force over **every contiguous row window of every sheet of all 8 workbooks** found no window producing the reported values - so this is not a merge and not a scope slip: the LLM summed a retrieved sample (~9-17% of true totals, ratios inconsistent). Confirmed in code: `D:\AirGPT\rag\answer.py` (1651 lines) contains zero occurrences of `groupby`, `GROUP BY`, `top_n` or even `category`; `rag/ingest.py:103` flattens every sheet to text for embeddings. **Root cause class: an LLM was permitted to perform arithmetic over a retrieved sample instead of a scan.** Founder direction same day: bring free-form search into DMS as a product feature (not AirGPT-only); target Genie / Cortex-Analyst-class free-form answering with no regex cascade; add MSSQL + MySQL ingest; DeepSeek as a model option; ontology -> insights -> generated apps, all orchestration decided by Cortex. | **EPIC-017** + **EPIC-018** (new, RUN NOW) + **EPIC-019** / **EPIC-020** (new, next) + **EPIC-015** (amend: L3 numeric bar) + **EPIC-021** / **EPIC-022** (recorded, deferred with condition) + **section 3 OOS amendment** (MSSQL/MySQL read extract in; live federation and CDC out) | **Sliced - Wave 7 added.** Metric change is the load-bearing decision: single "accuracy" retired in favour of **precision-on-answered (100.00%, law)** + **coverage (grows)**. 99.99% accepted as a **precision** target, rejected as a coverage target - comparator evidence: Cortex Analyst 90%+ *after* refusing ambiguous questions, Genie 100% on 13 curated questions and >80% bar before UAT, best agents 21.3% on Spider 2.0. Free-form-in-DMS is **not a new epic** - it is EPIC-015, already open under F17; amended only with the L3 bar (doc-RAG may not emit numbers). No new repo: 5 of 6 Cortex-Analyst mechanisms already exist across Cortex ontology pack + EPIC-012 verifier + FreeRoute rotate + hard rule 12. **Fixture defect logged** - eval corpus workbooks are clones sharing seven sheet names, so primary50 could not have detected this failure class. **NEEDS-YOU:** DeepSeek data residency / PDPA for L2 schema+literal exposure. External "replaces Palantir/Databricks" claim stays parked under H6 + section 8. |
| F27 | 2026-08-05 | founder / architecture ask (PRD Agent) | Should DMS replace DuckDB serving warehouse with Postgres or Cassandra/Scylla (CQL drop-in, higher throughput) for accuracy, throughput, streaming, and customer-DB migration? Alt: plugins so whole stack runs on MSSQL; or MCP into customer DynamoDB/MSSQL/"everywhere" to manipulate outputs, inference, ontology. | **none - PRD amendment / founder stop** (warehouse engine swap + live federation + MCP-into-customer-DB). **Partial already open:** EPIC-020 (`sql_source` MSSQL/MySQL -> bronze extract, F26). **Parking only:** P-DMS-17 (Iceberg/Spark on measured writer contention); P-DMS-28 (Cassandra/Scylla + MCP-write-to-customer-DB declined). Do **not** file Cassandra/Scylla tickets. | **Routed - no new epic, no tickets.** Accuracy != warehouse engine (F26: precision-on-answered + scan provenance). Postgres already = control plane (`cortex`+`dms` + pgvector), not a sixth serving store. `serving_engine` port (`ports.py:4`) is the only lawful DuckDB->warehouse swap later - not Cassandra (wrong query model for analytics+provenance). Live federation / MCP mutate customer DB / ontology-in-customer-DB = **forbidden** (section 3 OOS + Cortex-owns-ontology + five-port law). IN SCOPE now: Wave 7 EPIC-017/018 then 019/020 extract connectors. OUT: replace DuckDB, embed Cassandra/Scylla, run stack on customer MSSQL as serving plane, streaming brokers (last, behind bronze writer), MCP write path into customer DBs. **NEEDS-YOU RESOLVED 2026-08-05 founder:** **YES** decline Cassandra/Scylla as product path; **YES** decline MCP-into-customer-DB + "install as plugins inside MSSQL and build ontology there"; **EPIC-020 remains extract-only** (no live federation reopen). Cite: `Cortex/docs/subagents_findings/2026-08-05_dms-serving-warehouse-verdict.md`. |
| F28 | 2026-08-05 | founder + research-ingest (PRD Agent) | Accuracy depth/coverage without warehouse swap: learn from DeepMind-class agent papers, Databricks Genie/AI-BI, MNC DB systems, and Bigtable 20-year paper (SIGMOD Companion 2026 DOI 10.1145/3788853.3803095). Want: synonyms, acronyms, free-form, RAG spaces, strong Cortex orchestrator (DAG/graph, goal, verification, golden rules). Explicitly **not** more regex/prompt edge-case covering. | **Existing Wave 7** EPIC-017/018/019/020 + EPIC-015 L3 + EPIC-006 (orchestrator-shaped C7) + deferred EPIC-021; park paper backlog **P-DMS-29**. **No new epic.** | **Routed — no parallel product.** Map: (1) **Bigtable paper = storage ops** (replication, EB-scale, QPS, wide-column; Cassandra lineage) — does **not** lift answer precision/coverage; reinforces F27/P-DMS-28 decline; only adjacent unlock remains P-DMS-17 (measured Iceberg). (2) **Genie/AI-BI levers into Wave 7:** trusted assets / curated examples → **EPIC-019**; precision-on-answered + coverage instrument + benchmarks → **EPIC-018**; numeric authority / no RAG arithmetic → **EPIC-017** + **EPIC-015** L3 bar; synonyms/acronyms/entity matching → hard rule 12 + ontology/trusted assets (not regex); free-form + Space RAG already F17/F26. (3) **Orchestrator depth** (DAG/goal/verify/golden rules) → Cortex engine only (Wave 7 law); EPIC-006 stays open as cascade *replacement*, not regex growth. (4) **MNC DBs** → **EPIC-020** extract-only; **ontology inside customer MSSQL stays declined**. (5) Customer-shaped semantic layer stays **EPIC-021** (gated on EPIC-020). Paper-chase backlog → **P-DMS-29** (unlock: EPIC-018 instrument green OR founder promotes named papers). Accuracy path = Cortex orchestrator + semantic/trusted assets + eval instrument — **not** DuckDB/Cassandra/MSSQL-plugin swap. |
| F29 | 2026-08-05 | founder via Cursor (PRD Agent) | After every build: on-screen What's New sticker / smooth changelog popup; button-to-button guided tour; reusable section tutorials so users fully utilize features. Inspiration: Palantir ontology docs best practices + full Build-with-AIP examples catalog (Agent Studio, Workshop, Contour, Quiver, Pipeline Builder, OSDK, Cipher, writeback, media) at build.palantir.com / palantir.com/docs — founder phrasing: "copy every feature / screenshot / perfect UI / wire backend that makes them happen." | **Split.** (1) Thin SURFACE → **EPIC-023** (deferred, UI-only over existing Spaces/Chat/Studio/Library/Ontology + E9 honesty). (2) AIP Build-examples gallery + Workshop/Contour/Quiver/Cipher/OSDK/Agent Studio/Pipeline Builder/writeback/media **backends** → **none — H6 / section 3 OOS**; park **P-DMS-30**. Do **not** implement; do **not** file AIP-clone tickets. | **Routed — thin slice approved as deferred epic; fat half rejected under H6.** Verified surfaces exist: `SpacesPage`, `ChatPage`, `StudioPage`, `LibraryPage`, `OntologyPage`. EPIC-023 **approved deferred SURFACE, not RUN NOW** (WIP: Wave 7 residual 017/018 then 019). Ticket acceptance one-liners for epic-agent **when unlocked only**: **NEW-01** WHEN a new build opens the UI, THEN a dismissible What's New popup/sticker shows shipped changelog for that version without blocking ask; **TOUR-01** WHEN Help>Tour (or first-run), THEN button-to-button spotlight walks Spaces→Studio→Chat→Library→Ontology on real controls only; **TOUR-02** WHEN started from a page Help, THEN same engine runs as a section tutorial (no invented screens); **HONEST-01** tour/What's New copy SHALL name Space scope + abstain/E9 honesty and SHALL NOT claim Workshop/Contour/Quiver/Cipher/OSDK/Agent Studio. Style when built: pretty, curvy, minimalist, interactive — polish only. **Reject:** fake backends for AIP product clones. **NEEDS-YOU RESOLVED 2026-08-05 afternoon founder:** **A** — thin EPIC-023 only; **B rejected** (do not amend H6; P-DMS-30 stays parked; no Build-examples gallery programme). Priority: accuracy first; EPIC-023 not filed as tickets today. |
| F30 | 2026-08-05 | founder via Cursor (PRD Agent) — after F29; Palantir SuperRepo beta published 2026-08-04 | Palantir SuperRepo beta: pro-code monorepo holding Ontology definitions + functions + React app as one versioned artifact; local Foundry CLI preview (Ontology-as-code in TypeScript, embedded Ontology, auto-generated OSDK, seed data); deploy as Marketplace product (signed bundle) via platform CI or external CI (`env.yml`); can import existing Ontology Manager types. Coming later: Python functions, Agent engine/SDK, external sources, Automate, data pipelines. Pasted in F29 context (What's New / tour / Build-examples). | **Existing, not a tour.** (1) Internal SuperRepo-class path for MSSQL/MySQL+Excel shops → **EPIC-022** (still precision-gated on EPIC-018) + Cortex **P14** dual-brain / `ENGINE_SDK_DUAL_BRAIN_PLAN` + **P17** engine API/self-host packaging + packs ontology-as-code + O4 Agent SDK / O7 `new_pack.py`. (2) Foundry CLI / Marketplace / SuperRepo monorepo clone + TS OSDK codegen + signed Marketplace bundle → **none — H6 / section 3 OOS**; park **P-DMS-31**. **Not** EPIC-023. Do **not** approve clone this sprint. Do **not** file SuperRepo/Foundry-CLI tickets. | **Routed — no new epic; no clone; no tickets.** Verified already analogous: `D:\Cortex\packs\dms\ontology\{object,link,action}_types.yaml` + `functions.yaml` (ontology-as-code, YAML not TS); O4 `CortexOS/agent_sdk` (`query_objects` / `call_action`); O7 `scripts/new_pack.py`; DMS React app as **separate** consumer (`D:\DMS`) over HTTP `cortex_client` + compose image pin + `cortex-contract` wheel (versioned engine ≠ monorepo SuperRepo). Genuinely missing vs SuperRepo: single versioned monorepo artifact (ontology+functions+React), Foundry-CLI local preview with embedded Ontology + auto-gen TS OSDK + seed, Marketplace signed-bundle/`env.yml` deploy, Ontology Manager type import. Palantir "coming later" Agent engine ≈ Cortex O4/P14 already started — still not a Foundry Agent Studio clone. Internal goal already endorsed in Wave 7 positioning (self-serve Foundry/Genie for MSSQL/MySQL/Excel shops); external claim stays H6. **F29 NEEDS-YOU closed A** (independent of SuperRepo). |
| F31 | 2026-08-05 | founder via Cursor (PRD Agent) | Answer orchestrator should be "AI smart" and itself choose architecture: Gen / C / JEPA / FSM / DAG vs linear verify loop — strong architecture-level thinking. Continue research findings; build/land so founder can test; highlight what changed & what was tested; founder will invent new cool use cases meant to FAIL the system; existing cases insufficient / fail to stress real gaps — need harder adversarial use cases. | **No new DMS epic.** Adaptive chooser → Cortex gen-cFSM / **P21** research (cross-ack). Build priority stays Wave 7 **EPIC-017/018** land + hostile eval pack under 018. Park ask-path architecture selector **P-DMS-32**. Do **not** invent a sixth DMS port. Do **not** file gen-cFSM tickets in DMS. | **Routed — research/Cortex, not this sprint's product path.** Law: prefer linear FreeRoute verify loop on DMS ask (F28); DAG only if EPIC-018 shows bottleneck. Cortex already has gen-cFSM G1.0/G1.1; enterprise JEPA→DAG→telemetry loop is **P21** (`ENTERPRISE_GEN_CFSM_LOOP_PLAN.md`) — honesty: never claim trained JEPA shipped. Chooser must not bypass E1–E9, abstain-over-invent, or five ports. **Test NOW:** `score_answers` + envelope assertions; land 017/018 with adversarial use cases designed to fail green (existing corpus insufficient — F26 fixture defect class). Unlock for ask-path meta-chooser: EPIC-018 green + measured linear-refine bottleneck **OR** founder names ask-path policy switch. **Next agent:** epic-agent / ticket-runner for EPIC-017/018 only. |
| F32 | 2026-08-05 | founder via Cursor (PRD Agent) — hostile/primary50 DMS ask | User asked DMS `"show top 3 categoty sales"` (typo). DMS returned **Home 383,803.56 / Sports 242,755.97 / Misc 228,548.84** via Wide_Fill / multi-source RAG (sources: `cf98e431_p50_01_sales_messy`, `95018ba9_p50_02_ops_messy` Sales, `aa64458a_p50_03_inventory_messy` Wide_Fill, `0e9d7a15_p50_04_mixed_messy` Summary). Hand-filter truth: Home **1,199,018.49**; Electronics **1,545,366.4**; Misc **380,948.33** (sales_messy Sales); inventory figure **1,563,234** (context unclear). Same magnitude pattern as F26 AirGPT failure; founder wants iterate toward ~99% accuracy. Also: (a) Cursor Skills cards — remove descriptions; purple tags hover-only / real actions; (b) RAG/"Primary 50MB Bench" previous-chats card — need summary; text color = card bg (no contrast); (c) Excel hanging/skipped row when filter-sum — does blank row break DMS ingest/answer? | **Open Wave 7** — **EPIC-017** (#33) + **EPIC-018** (#35) + **EPIC-019** (#38 / VQ-01#39 VQ-02#40). **Do not reopen** SCORE-01/02 (#36/#37 CLOSED). **No new epic.** **No PRD amendment** for "~99% accuracy" (already Wave 7: precision-on-answered **100% law** + coverage grows; 99.99% is precision not open-domain coverage — F26). Cursor Skills + Agents Chats contrast → **none / OOS** (Cursor product chrome, not DMS ChatPage / not Pointer unless founder explicitly remaps). Excel blank/hanging row → optional hostile check under **EPIC-018** only (triage already has blank-band in `packages/executor/dms_executor/triage.py`); not a new epic. | **Routed — same F26 class now on DMS ask path.** Cite STATUS: SCORE-01/02 closed; VQ-01/02 + EPIC-017/018/019 open; accuracy = verified assets + semantic metrics + execution-verify (not regex). Cite `scripts/score_answers.py` fail modes: `wrong-sheet scope` (Wide_Fill vs Sales), `RAG-sum trap` (F26 invent partial totals), Home **1199018.49** as Sales truth in `tests/invariants/test_envelope.py`, **383803.56** as wrong confident pattern. **Acceptance framing for epic-agent tickets:** ambiguous typo ask without sheet scope SHALL NOT render confident Wide_Fill/sample totals; lawful outcomes are (1) abstain/clarify/non-confident badge naming multi-sheet ambiguity (E9 / EPIC-017), or (2) executed warehouse ranking on declared Sales scope matching recomputed oracle magnitude+rank (EPIC-018), with synonym/typo `categoty`→category via curated assets not regex (EPIC-019). **SCORE closed correctly** — instrument documents fail_mode; pack staying red until VQ/trusted assets is intended. If live ask still ships confident invented/wrong totals after E9-01 closed, that is **EPIC-017 completeness failure** (decomposition miss on ask path), not SCORE reopen. **Next:** epic-agent completeness on #33/#35 then cut tickets under 017/018/019 as needed. **Do not** file DMS tickets for Cursor Skills/Chats UI. |
| F33 | 2026-08-06 | founder priority "I need the demo of free form prompting soon" (PRD Agent) | Free-form (L2 FreeRoute) works on a live stack: `verify_l2_vs_l1.py` ok:true, "rank carriers by on-time percentage for hazmat only" returns `L2_VALIDATED` with executed SQL and 6 rows; `verify_demo_live.py` 31/31 warm; `-EnableL2` + `DMS_L2_MODEL=auto` per `docs/L2_MODEL_BAKEOFF.md`. Question routed was not "does it work" but "what is missing to demo it to a buyer without it lying". | **EPIC-017** (#33, badge fail-open, close first) then **EPIC-003** (#6, Space boundary on the L2 path) + **EPIC-008** (#8, demo asset gains a free-form act). **No new epic. No PRD amendment.** Free-form-in-DMS already routed by F17/F26; L2-on-demo already an ops default (F17). EPIC-018 instrument reused as-is, **not** reopened. **Park EPIC-019/VQ-01** while the founder's Cortex lane is live. | **Routed - three measured gaps, none of them "L2 does not work".** (1) `verify_l2_vs_l1.py:131` accepts an L2 answer on `badge in expect_badges and not abstained` and **never compares a value to an oracle** - so precision-on-answered for free-form is today measured on **zero** questions (n=0, not n=1; R-0010 bounds nothing). (2) `_BADGE_MAP.get(..., "L2_VALIDATED")` at `packages/executor/dms_executor/__init__.py:448` and `envelope.py:370` **fails open**: any Cortex badge string DMS does not know renders as "This SQL was generated, then checked" (`AnswerMessage.tsx:43`). E5 cannot catch it because the default is already in `ALLOWED_BADGES`. The founder is editing Cortex now, so the badge vocabulary can drift under the demo. (3) Every Space-boundary envelope test in `tests/test_space_boundary_envelope.py` mints `badge="certified"` - **zero coverage of the L2 free-form path crossing a Space**, and `l2.retrieve_schema(question)` (`D:\Cortex\CortexOS\dms\answer_engine.py:2029`) takes no `space_id`. The boundary is the moat (NETIE.md 3.1) and it has never been shown to hold on the path being demoed. **NEEDS-YOU:** free-form on stage runs over the **seeded demo warehouse**, not a freshly uploaded messy workbook, until EPIC-019 lands - that constrains the sales story and is a founder call. |
| F34 | 2026-08-06 | verifier lane, cold-start reproduction (PRD Agent) | dms#43 DEMO-COLD-01: the **first** `verify_demo_live.py` run against a freshly started stack refuses the file it had just uploaded (403 `grounding_not_grantable`); every warm run passes. Candidate mechanism UNCONFIRMED: `packages/executor/dms_executor/demo_grants.py:93-98` catches any exception from `list_bronze_tables`, logs a warning, and returns an empty grant tuple. | **EPIC-008** (#8, demo asset) - **not** EPIC-003. **No PRD amendment.** | **Routed to the demo epic, deliberately.** The boundary is not breached: the grant path fails **closed**, which is the safe direction, so EPIC-003's acceptance ("the boundary holds") is intact. What is breached is **hard rule 11 / R-0011** - a read failure is silently re-presented as a permission decision, and the misattribution is the defect, not the read failure (R-0004: fix the class). It breaks the demo's own happy path (start, upload, tick, ask) on a cold laptop, which is EPIC-008's acceptance. Lawful outcome: a warehouse read failure SHALL surface as an error naming the read failure, never as `grounding_not_grantable`; the cold-start ordering fix alone is a symptom fix. Cold-start reproduction (delete `data/dms_demo.duckdb`) is running in a separate lane - the adversary is not the verifier (R-0003). |
| F35 | 2026-08-06 | CI observation (PRD Agent) | dms#44 CI-03: the `protected-paths` gate intermittently reports a missing `INVARIANT-CHANGE:` trailer that its own log prints. Green on re-run. A SIGPIPE/pipefail hypothesis was tested 300x and did not reproduce. Deliberately not patched - it fails closed, and a speculative fix to a control can mask the real fault. | **none - ops / repo hygiene.** Not an epic, not a PRD amendment. Precedent: F24(b) stack stability routed to "none - ops". **PARKING_LOT.md** entry with an unlock condition; #44 stays open as the record. | **Routed - no ticket now, and the reasoning for not patching is endorsed.** Correction to the framing though: a control that refuses legitimate work is a failure, not a win (**R-0005**) - a PR that carries the trailer being told it does not is exactly that, so "fails closed" is not a clean bill of health, only a reason it is not urgent. Unlock condition to write into PARKING_LOT: **it recurs a third time, OR it costs more than one re-run on a single PR, OR it fires on a PR that does not touch a protected path.** Until then do not patch (R-0007 class: a speculative fix to a checker buys false confidence). |
| F36 | 2026-08-07 | founder direct (PRD Agent) | Direction shift, verbatim: "the direction is heavily shifting, our main users are companies with sql servers like microsoft sql and mysql and a lot more so we need ingest data then perform query immediately or if best we become a plugin that can be called and appear in people's microsoft sql server". Two different products in one sentence: (a) connector + sync over customer MSSQL/MySQL, (b) DMS as a callable plugin appearing inside MS SQL Server. | **Split four ways, three of them not sliceable by an agent.** **(1) Target customer** (Excel-first Malaysian SME -> shops already running a governed RDBMS) = **Tier 3 amendment** to section 1 press release, section 2 external FAQ "What does it run on", section 3 OOS. Drafted, **not applied** - founder owns Tier 3. **(2) "ingest then query immediately"** = **EPIC-020**, already sliced under F26. Not new scope; the ask is a **priority change** - EPIC-020 leaves last-in-Wave-7 and becomes wave foundation. Becomes an amendment only if "immediately" means querying the customer DB live. **(3) "plugin ... inside microsoft sql server"** = **reversal of F27**, which the founder closed YES to declining on 2026-08-05 (`P-DMS-28`). **Not sliced. NEEDS-YOU.** **(4) Provenance for a non-file source** = **decision record first**, no epic. | **Routed - no epic opened, no PRD text changed.** Code-verified: DMS has **zero** MSSQL/MySQL client code (`sql_source` matches only `vendor/`); `SourceGrant.kind` at `packages/executor/dms_executor/acl.py:20` already reads `xlsx / parquet / sql`, so the grant model anticipates it. **Architecture verdict for (a): it survives.** The manifest carries `row_predicates` (table -> SQL boolean) and `allowed_paths` over **DMS's own** DuckDB lake (`manifest.py:52-63`); Cortex injects the predicates and DuckDB executes, so extract-to-bronze changes the *origin* of a bronze table and nothing about the boundary. **Verdict for (b) and for live federation: it does not survive.** The enforcing principal becomes the customer's SQL Server, which already granted those rows to the caller, so the Space boundary degrades to advisory unless DMS holds its own narrower credential - a customer provisioning requirement, not a code change. Cortex's hostile-SQL list (`manifest.py:269-285`) is DuckDB-dialect (`read_parquet`, `ATTACH`, `PRAGMA`) with no T-SQL coverage at all (`OPENROWSET`, `xp_cmdshell`, linked servers). **Obsolescence check against the founder's list:** hard rule 5 (Excel source-only) **stands and needs a sibling** (customer DB is read-only source, no writeback) - it does not die; hard rule 7 (`duckdb.execute` in executor) **stands under (a), dies under federation** - this single line is what separates the two products; bronze ingest UX **stands** and becomes *more* legible, not less (see F37). **Sharpest technical risk:** `_src` is `STRUCT(ref_id, row)[]` with `row = row_number() OVER ()` (`bronze.py:198-205`) - a row ordinal inside a DMS-held immutable file. Against a live remote table that ordinal is not stable across extracts and points at nothing the customer can look up, so "click any figure and see the rows" needs the extract snapshot retained and `ref_id` naming the **extract run**. That column is in every bronze table; changing it after data lands is a warehouse rewrite, which is why it ranks above the connector. **Epic re-priority:** **EPIC-016** (#29-32, Excel Copilot via Pointer) becomes the most misaligned open epic - unstarted; **park, do not close** (the F25 authorization must survive). **EPIC-019** (#38/#39/#40) **rises** - trusted assets are the only tractable coverage lever against a real customer schema - but its corpus is now wrong-shaped, built against the demo warehouse; re-point it at a real schema rather than curate assets that get thrown away. **EPIC-014** (#19, MCP) **rises** and is the only reading of "callable plugin" that does not reopen F27, **but MCP is not "appears in MS SQL Server"** - SSMS does not speak MCP, and that gap must not be papered over. **WIP breach recorded:** 7 epics open (#6 #8 #19 #29 #33 #35 #38) against a limit of 2. **NEEDS-YOU:** (i) is F27 reversed, specifically "MCP / plugins / ontology inside customer MSSQL"; (ii) does "query immediately" mean fast-to-first-answer over an extract, or live federation; (iii) approve or reject the drafted press-release amendment; (iv) does the Excel/SME segment stay, or is this a replacement. |
| F37 | 2026-08-07 | founder direct (PRD Agent) | "Involve a basic ETL tool or something like at least visualisation to let ppl to see how they change from bronze to silver to gold". The medallion transformation is invisible to the user today. | **No open epic owns it** - checked EPIC-003 (boundary), EPIC-008 (demo asset), EPIC-017/018/019 (accuracy), EPIC-023 (UI-only tour over existing surfaces, no backend data). By the routing law that makes it a **new epic under PRD-001, founder to approve** - proposed **EPIC-024**, repo `Netie-AI/dms`, contract impact **none**, depends on nothing. **No PRD text amendment needed:** section 1 already promises the rows behind a number, and the ingest-honesty thesis (triage receipts, quarantine reasons) is already recorded in `PARKING_LOT.md`. Closest parked item is **P-DMS-12** (interactive warehouse UX: "Runs timeline", "ETL action confirm") - this is its unlock, not a new lane. | **Routed - not an ETL tool, a rendering epic.** The data is already computed on every promote and then discarded. `PromoteReceipt.to_dict()` in `packages/core/dms_core/pipelines.py` returns `run_id, target, sources, source_rows, passed, quarantined, unmatched, reconciled, counts_by_reason, dedup_key, quarantine_table`; `POST /v1/pipelines/run` (`apps/api/dms_api/routes/pipelines.py:39`) returns it verbatim behind `compliance_gate`; and `apps/ui/src/lib/api.ts` contains **zero** pipeline functions - nothing in the UI has ever called it. **Surface answer: Library, not Studio, and not a new page.** `LibraryPage.tsx:134` already models the tiers as `folder:sources / folder:bronze / folder:warehouse`, so Library is the read surface; Studio keeps the promote **action**, because it is a gated mutation. A sixth page is not warranted for data that has a home. **Do not build an arrow diagram.** dbt and Airflow render the arrow for free; nobody renders `reconciled: false` with the rows that never arrived. The differentiated view is `source_rows -> passed + quarantined`, `counts_by_reason`, and **`unmatched`** (negative means join fan-out) - the same instrument that caught the ~15x oracle inflation on `03bcf2d` today. **Interaction with F36:** they reinforce **under (a)** and conflict **under live federation**. A bronze table extracted from MSSQL is a snapshot of a table the customer already knows by name, so the bronze-to-silver delta becomes legible for the first time; and a normalised customer schema fans out on joins **by default**, which makes `unmatched` load-bearing rather than incidental. Under live federation there is no bronze, no promote and no receipt to render, so F37's scope is contingent on F36 NEEDS-YOU (ii). Cheap either way, because (a) is the only lawful option today. **Pairing:** this is the human-inspectable half of the WIP pair for the SQL-source slice, which is otherwise a script and a number. |
| F38 | 2026-08-07 | founder direct, retrospective (PRD Agent) | dms#43 DEMO-COLD-01 already **fixed and closed** (`42a0fb5`, merged `3c6822a`); founder asked that the fix be routed for the record. **Correction to F34:** the filing basis there - `demo_grants.py:93-98` swallowing a `list_bronze_tables` failure - was **disproved by experiment**, not merely left unconfirmed. Actual cause: a cold engine's first submit times out, classifies as `submit_failed`, and the chat route's status expression was `409 if code in {session_unbound, session_expired} else 403`, so **403 was the default branch**; `result.error` was additionally discarded, so a timeout and a policy refusal were indistinguishable. | **EPIC-008** (#8) retrospectively, **unchanged from F34** - the epic assignment was right for the wrong reason, and the ledger keeps both readings. It broke the demo's own first question on a cold laptop, which is EPIC-008's acceptance, and the boundary was never breached. **Second owner, newly named: EPIC-017** (#33) for the completeness gap below - flagged as a **widening**, founder may veto. **No PRD amendment.** | **Routed - and it does imply a rule, but not one R-0011 already covers.** R-0011 / NETIE.md rule 6 catches a degradation that is **invisible**. This one was maximally visible - an HTTP 403 carrying a message - and wrong about **which kind** of failure it was. Proposed rule for Netie-KB, not a PRD line: **a failure may not be expressed in another failure's vocabulary, and the default branch of an error classifier must be the least-attributive outcome available.** A status that asserts something about the **caller** (403: you may not) must be **earned by an enumerated code**; a status that asserts only about the **system** (502) is the only lawful default. Enforced in DMS by `_STATUS_BY_CODE` in `apps/api/dms_api/routes/chat.py` plus the two status tests in `tests/test_live_ask.py`, and falsified before trusting green (R-0007): restoring the 403 default fails both. **The completeness gap, stated plainly:** envelope assertions E1-E10 all run on a 200 answer. A refusal is also an artifact the customer receives (NETIE.md rule 1), and **no assertion covers the shape of a non-200** - which is precisely why a 403 could carry a timeout for as long as it did. **Third item worth keeping:** the first warm-up attempt asked with `space_id: "warmup"`, 404'd on `space_not_found` in under a second, and printed "Answer path warm (0s)" having warmed nothing - a gate that cannot fail (R-0007) reappearing as a warm-up that cannot warm. Measured cold submit 9.5s, later 30.8s on a slower start; the launcher now resolves a real Space and warns if the round trip returns too fast to have reached the engine. |
| F39 | 2026-08-23 | founder via Cursor `/loop` `/goal` (PRD Agent) | Verbatim class: "scale open infinite subagents solve all different tickets non blocking after done check verify gating and improve and debug and harsh and build and check on ci/cd debug then merge then clear non block /loop /goal final working best dbms with strong ontology and platform like palantir and databricks". Two asks glued: (1) **product** = working DBMS + strong ontology + Palantir/Databricks-class platform; (2) **ops** = parallel non-blocking tickets, verify, gating, harsh debug, CI/CD, merge, clear. Re-ask of F26/F28/F30/F36 (and the unlogged vision lane named in `~/.claude/subagents_findings/2026-08-23_f39-ontology-paas-vision.md`). | **Existing only — no new epic.** Product: **EPIC-019** (#38/#39/#40 trusted assets) owns "strong ontology DBMS" together with already-landed grain compiler `D:\DMS\scripts\ontology.py` (DR-0003 class) + deferred **EPIC-021** (customer-shaped semantic, gated on EPIC-020). Connectors = **EPIC-020** (blocked F36). Generated apps = **EPIC-022** (precision-gated, stay parked). Real PaaS = Cortex **P17** parked; H6/P1 stay parked. Ops = AGENT_SYSTEM ticket-runner loop (verify then gate then merge) — **not** a DMS epic. **No Palantir-PaaS / Foundry-clone / Databricks-clone epic. No new tickets.** | **Routed — re-ask, not missing scope.** Nearness (artifact: `STATUS.md` 2026-08-23): demo 31/31 + AW lake ontology verified + `ontology.py` grain guard in tree; gap that blocks "best DBMS" claim is confidently-wrong **#57** (FF-02, PR #61), not connectors. **Owner of strong ontology:** EPIC-019 (curated verified queries / trusted assets) + strengthen `scripts/ontology.py` / `ontology_bench.py` (Databricks metric-view / Palantir derived-property class) + EPIC-021 when EPIC-020 has one real schema. Do **not** merge Constructor (Cortex consumer skin) with `ontology.py`. Kinetics = `action_types` + `dag_runner` NodeType, not canvas physics. **RUN NOW:** #57 then #59, then remaining READY non-blocked (#40, #28, #18/PR#64, #8/#6 residuals as READY). **PARKED:** EPIC-016 #29-32; EPIC-022; H6 / Cortex P1 / P17; EPIC-020 SQL-shaped until F36; #39 blocked Cortex in-flight; MCP #19-21 blocked; F27 plugin-inside-MSSQL stays declined. **NEEDS-YOU unchanged:** F36 (F27 reversal? extract vs live federation? press-release amend?) + F37 approve/reject EPIC-024. External "Palantir" stays section-2 Q / H6 until paying client + F-gates. Ops ask: run ticket-runner with WIP discipline (at most two epics; one human-inspectable); do not invent an "infinite subagents" product surface. |
| F40 | 2026-08-23 | engine-side accuracy intake (PRD Agent) - DR-0003 open finding, item (a) | **Refusal-badge P0.** A Cortex manifest refusal (`PathNotAllowed` / `StatementNotAllowed` / `SqlNotAnalyzable` - the strongest "no" the engine has; nothing read) reaches `POST /v1/chat/ask` as `badge=L2_VALIDATED`, `abstained=false`, E1-E9 green. Reproduced today: `python D:\DMS\scripts\repro_refused_badge.py` exit 0 - LINK 1 `_is_abstain_signal(refusal)=False`, LINK 2 envelope `L2_VALIDATED / abstained=False` over the text "That question can't be answered inside this session's data grant (PathNotAllowed...)". | **EPIC-017** (#33, open, in flight) parents the DMS half; **Cortex EPIC-001** (Netie-AI/Cortex#11, open - acceptance "receives a refusal when the question reaches outside that Space") parents the Cortex half. Second owner **EPIC-003** (#6): this is the F33 (3) missing L2-path boundary coverage. No new epic. No PRD amendment. Contract impact **none** (`Badge.ABSTAIN` is already on the wire). | **Routed - RUN NOW, the cheapest P0 on the board; runs alongside #57 inside EPIC-017.** Code-verified: `CortexOS/api/contract_routes.py:118-125` knows `needs_clarification / abstain / blocked` and not `refused`; `:148` `_FLAT_BADGE.get(badge_raw, Badge.SESSION)` defaults an unknown badge to a **confident** one - the exact class F33 (2) fixed in DMS (`normalize_badge`: unknown -> ABSTAIN), one hop upstream. DMS then maps `session -> L2_VALIDATED` (`envelope.py:31`) and derives `abstained` from the normalised badge. Why no gate caught it: E9 needs an unbacked number (refusal text has none), the hard-rule-12 net needs `sql_used` (None), and `assert_envelope_valid` only checks internal consistency. R-0004: fix the class (unknown -> ABSTAIN in Cortex) and the word (`refused` is an abstain signal) - not a DMS special case for `layer == "refused"`. Sequence: Cortex ticket -> DMS ticket (the repro becomes a test on the envelope: `abstained=true`, `ABSTAIN`, text names the refusal, for a Space-scoped ask that reaches outside the Space). Epic-agent files both; EPIC-003 completeness re-runs the DMS test. Until it lands, success-assertion clause 1 ("return an abstention naming the refusal") does not hold on this path. Cross-ack written into the Cortex PRD ledger (F11). |
| F41 | 2026-08-23 | engine-side accuracy intake (PRD Agent) - DR-0003 items 1-2, item (b) | Grain guard / ontology compiler **into Cortex**: measures declare grain; compile refuses fan-out and ambiguous paths; multi-hop with `via=`; `verify()` measures cardinality instead of trusting it. Prototype `D:\DMS\scripts\ontology.py` (957 lines) + `tests/test_ontology.py`; `scripts/ontology_bench.py` 431/431, 7 correct refusals over 3 AW databases; AW lake 110 objects verified. | **none - PRD amendment + DR-0003 acceptance; founder.** Nearest clauses: deferred **EPIC-021** (customer-shaped semantic / metric layer; unlock = "EPIC-020 has landed one real customer schema" - not met, and EPIC-020 is itself blocked on F36) and the Wave 7 sentence "The product gap is curation UX and the L3 bar, **not engine capability**" - which DR-0003 contradicts with measurement. F39 authorised strengthening the DMS scripts; it did not authorise wiring a compiler into the engine's L1 path. | **STOP - founder decision; epic drafted, not opened.** Verified 2026-08-23: 118 tests pass across `tests/test_ontology.py`, `test_stream_ingest.py`, `test_insights_brief.py`, `test_freeform_gate.py`. Cortex's live semantic layer is `packs/dms/semantic/metrics.yaml` + `route_to_metric`, with `packs/dms/ontology/link_types.yaml:4` declaring `many_to_one` by hand ("All joins are FK -> PK, so many_to_one") - declared, never measured; the demo warehouse's own `inventory` is 7,388 rows for 509 SKUs (the ~15x inflation class). Three open Cortex bugs are this class under other names: Cortex#36 ANS-02, #38 ANS-03, #39 ANS-04 (the router answers the adjacent question under `governed_metric`; #38 already names the fix: "the router states the shape its plan returns, and refuses"). Why founder, three reasons: (i) DR-0003 is `status: proposed`, decision-makers "founder (pending)" - a proposed DR authorises nothing; (ii) accepting it amends the Wave 7 "not engine capability" sentence (Tier 3); (iii) the honest slice is to **split EPIC-021**: **EPIC-021a** "grain-guarded metric compiler in the engine" - Repo: Netie-AI/Cortex; Contract impact: **none** (L1 stays `L1_GOVERNED_METRIC`; a fan-out becomes a refusal); Depends on: nothing; invisible FOUNDATION, so it must pair with a visible epic (EPIC-017 now, or F45 (B) later); acceptance on the DMS envelope: WHEN a governed measure is asked below its declared grain or across an unverified or ambiguous link THE SYSTEM SHALL refuse naming the grain or the ambiguity, AND every grouped total SHALL conserve to the raw fact total with no join - and **EPIC-021b** customer-shaped model, condition unchanged. Changing a founder-stated unlock condition is not the agent's call. Hard rule 6: no new port or dependency - the compiler replaces hand-declared cardinality inside the existing semantic pack. **Decision asked:** accept DR-0003 items 1-2 and open EPIC-021a now, or keep the whole of EPIC-021 gated on EPIC-020. Until answered `ontology.py` stays a DMS script; Constructor must not import it (F39). |
| F42 | 2026-08-23 | engine-side accuracy intake (PRD Agent) - DR-0003 item 7, item (c) | Generated engine benchmark in CI (`ontology_bench.py`) and, more to the point, **no accuracy gate of any kind runs in CI today**. | **EPIC-018** (#35, open) - a completeness gap, not a widening: its acceptance says "WHEN the eval suite runs ... SHALL fail the run on a single confidently-wrong answer", and DOCUMENT_SYSTEM section 8 makes "checked in CI" the mechanism that turns doc drift into a build failure. The `ontology_bench.py` half rides **F41** (NEEDS-YOU). | **Routed.** Verified `.github/workflows/ci.yml`: jobs `protected-paths`, `contract-pin`, `lint-type-test` (ruff / mypy / import-linter / `pytest tests/`) - zero oracle gates; every accuracy instrument (`verify_freeform_demo.py`, `score_answers.py`, `ontology_bench.py`) is manual. Framing for the epic-agent: stack-free halves first (`verify_freeform_demo.py --self-check` / `--oracle-only`, `score_answers.py --oracle-only`) labelled as oracle self-tests; the product-path half needs the stack in CI and must say so - a job called "accuracy" that asks nothing is an R-0011 lie; prove the job can fail (R-0007) before trusting it green. `ontology_bench.py` in CI bounds a DMS script's error rate (<=0.70% at n=431 by R-0010), not the customer's envelope (R-0001) - it becomes an engine acceptance instrument only if F41 opens EPIC-021a; until then it is a unit test of a prototype and is labelled as one. |
| F43 | 2026-08-23 | engine-side accuracy intake (PRD Agent) - item (d) | Incremental ingest with atomic visibility: `scripts/stream_ingest.py` + `tests/test_stream_ingest.py` - a half-loaded slice is never queryable (staging -> assert -> one atomic manifest write), `(low, high]` watermark with both ends stored, identity sweep for NULL-watermark / late / boundary / rejected rows. Founder ask: batch and streaming intake as a product capability. | **EPIC-020** (`sql_source` extract connector - sliced under F26, **no GitHub issue yet, blocked on F36 NEEDS-YOU (ii)**). Section 3 already authorises exactly this shape: "a scheduled incremental extract with a **visible freshness watermark** is the supported answer to 'my numbers are stale'". True CDC / streaming brokers stay **out** (section 3 + F27 closed). No new epic. | **Routed - in scope as the incremental half of EPIC-020; not startable.** The mechanism is source-agnostic (`run()` / `ingest_batch()` library entry points; the CLI deliberately exposes only `--demo` and `--status`), so it is the extract connector's loop rather than a product of its own, and the visible freshness watermark is its human-inspectable half (the WIP pair). If "streaming" means CDC or a broker, that is a section 3 amendment - founder. Not checked here and worth the epic-agent's EPIC-003 completeness question: whether today's multi-sheet upload can leave earlier sheets queryable after a mid-workbook failure (P0-DEMO-01 class, dms#4 closed). |
| F44 | 2026-08-23 | engine-side accuracy intake (PRD Agent) - item (e) | `.bak` restore -> lake -> validate: `scripts/load_adventureworks.py` (throwaway SQL Server container restores the backup, extracts to Parquet; nothing talks to SQL Server after exit) + `scripts/validate_lake.py` (row_count / pk_null / pk_unique / fk_intact / **fk_cardinality derived not declared**, DuckDB over Parquet). Founder ask: load real SQL Server `.bak` datasets. | **Split.** As a **test asset** -> **EPIC-018** (#35): PRD Wave 7 already names AdventureWorks "the referential-integrity oracle; a test asset, not a runtime dependency" - authorised, no ticket unless CI needs the fixture (F42). As a **product capability** (a customer's database entering DMS) -> **EPIC-020**, blocked on F36. F27 stands untouched - this lane is extract-only by construction and reverses nothing. | **Routed.** Lake verified present: `data/lake/AdventureWorks2025` 82 MB, `AdventureWorksDW2025` 38 MB, `AdventureWorksLT2022` 12 MB - not in git (release ideology: no binaries). PR #65 ("extract-lab follow-up", founder-run) puts the scripts on main with `pyarrow>=14` in root `[project.dependencies]` and `AW_IMAGE=mssql/server:2025-latest` - lawful as test tooling; two notes for the ticket-runner: `pyarrow` as a runtime dependency has no stated swap scenario (hard rule 6), and the `.bak` container must never become the product's ingest path - a customer restores on **their** server and EPIC-020's connector extracts read-only to bronze. STATUS hold stands: nothing SQL-Server-shaped starts until F36 is answered. |
| F45 | 2026-08-23 | engine-side accuracy intake (PRD Agent) - item (f) | Insights + brief: `scripts/insights.py` (declared measures x discovered dimensions; concentration / dominance / unknown-bucket / refusal templates; every figure compiled through the grain-guarded compiler and conserved; **no model**) -> `scripts/brief.py` pptx / html; `tests/test_insights_brief.py` reads the deck back and every number must be in the report. Founder direction: Databricks / Palantir challenger - insights, auto-generated briefs. | **none - PRD amendment; founder.** Checked: **EPIC-013** (Netie-AI/Cortex#29, CLOSED) was per-answer insight bullets + chart_spec after rows - a different capability, its decomposition did not fail, so no reopen. **EPIC-022** (generative artifacts over the ontology) is the only clause that fits and it is gated "on precision, not on time": EPIC-018 reports 100.00% precision across two consecutive waves with coverage >= 60% - not met (STATUS: coverage low; #57 confidently-wrong open). Section 3 H6 / P-DMS-30 park the Palantir-parity surfaces. | **STOP - drafted, not opened.** The gate's reason ("generating over an unverified metric layer mass-produces confidently wrong dashboards") applies to a brief exactly as to an app; the prototype answers it only because every figure passes the grain guard - which is not in the product (F41). So (f) is downstream of (b): lawful only after EPIC-021a lands in the engine. **Decision asked:** (A) keep under EPIC-022's precision gate - default, nothing opens; (B) amend: a thin **deterministic** insights + brief epic - Repo: Netie-AI/dms; Contract impact: none; Depends on: EPIC-021a; acceptance: WHEN a brief is generated for a Space THE SYSTEM SHALL put no figure on a slide that is not an executed, conserved query result, asserted by reading the deck back - a visible SURFACE and the natural WIP pair for EPIC-021a; (C) decline. Hard rule 6 note: `brief.py` imports `python-pptx`, which is not in `pyproject.toml`; the one invocable Cortex action today is a PowerPoint export, so the product path for a deck is that action type through `call_action`, not a second pptx writer in DMS. Internal positioning (self-serve Foundry / Genie class) stays endorsed; external "Palantir" stays H6. |
| F46 | 2026-08-23 | engine-side accuracy intake (PRD Agent) - DR-0003 items 2-6, 8, item (g) | DR-0003 remaining items: 2 cardinality verified at build; 3 schema allow-list validated pre-execution; 4 value dictionary; 5 verified answers (pinned parameterised plans); 6 refusal badge; 8 model promotion scored on correctness. | 2 -> **F41** (part of EPIC-021a, NEEDS-YOU). 3 -> **existing Cortex#6 SEC-01** under Cortex EPIC-001 (#11): manifest enforcement on the product path **is** the allow-list at table level; column-level allow-list is the residual gap and is named there, not a new epic. 4 -> **EPIC-019** (#38), Cortex-side inside VQ-01 (#39) scope, plus a verify-first item under **EPIC-017** (#33): an impossible filter certifying `0`. 5 -> **EPIC-019** (#38) VQ-01 / VQ-02 - already its acceptance; pinned parameterised plan and "`L0_CERTIFIED` only that way" recorded as constraints. 6 -> **F40**. 8 -> **EPIC-018** (#35): promotion scored by the instrument (precedent F33 (1); not an EPIC-012 reopen). | **Routed - no new epic, no PRD text changed.** Evidence: `envelope.py:589-601` withholds an empty result set and exempts one-row results, and `SUM` over a filter that matches nothing is a one-row `NULL` - verify on the live envelope before ticketing. `scripts/bakeoff_l2_models.py` promotes on badge + latency at n=1. Cortex#6's body predates `answer_engine.py:1388` ("semantic guardrail then C4 submit (enforce_manifest)") - the epic-agent re-derives its state from code before filing anything under it. Epic bodies #33 / #35 / #38 carry the routed bullets. |
| F47 | 2026-08-23 | housekeeping found during intake (PRD Agent) | Three open tickets had no live parent: **#57 FF-02** and **#59 FF-03** (no Parent line at all, both RUN NOW), **#18 CSV-01** (Parent EPIC-013, CLOSED). DOCUMENT_SYSTEM Tier 7: a ticket with no live parent may not be worked. | #57, #59 -> **EPIC-017** (#33): its acceptance is a confident badge only over a provenance-backed answer, or an abstention **naming why** - #57 is a confident wrong answer on the served path, #59 an abstention that throws its why away. #18 -> **EPIC-008** (#8): demo-surface polish (F13: "polish only if broken under demo"). | **Re-parented - bodies of #33 and #8 carry them; comments on #57 / #59 / #18.** Not a scope change. WIP after this intake: in flight = **EPIC-017** (#57, #59, F40 P0) + **EPIC-018** (F42, F46-8); EPIC-008's residual (#18 PR #64, #28) is tail closeout and counts, so the ticket-runner holds two and closes EPIC-008 before lighting EPIC-019. EPIC-019 (#38) stays queued (VQ-01 blocked on Cortex in-flight), EPIC-003 (#6) open / queued, EPIC-014 blocked, EPIC-016 parked. |
| F48 | 2026-08-23 | nearness check - every run (PRD Agent) | Artifacts checked: `D:\DMS\STATUS.md` (2026-08-23: `verify_demo_live.py` **31/31** on a cold stack; free-form "coverage is low and everything answered came from L0/L1, not L2"; one confidently-wrong open, #57); `apps/ui/src/pages` = 10 app pages (Spaces, Chat, Studio, Library, Ontology, Audit, Runs, Trust, Amend, Admin); the public landing https://netie.ai/ (repo Netie-AI/landing, pushed 2026-08-22), fetched 2026-08-23; `python scripts/repro_refused_badge.py` exit 0. | **none - measurement only.** Marketing owns the landing (Netie-AI/landing); PRD section 8 ("nothing in the press release appears in an external asset until it traces to a passing gate") is the governing clause. | **Measured; no percentage.** From the customer's seat: the app path exists and the demo holds (31/31); success-assertion clause 1 fails on the manifest-refusal path today (F40) and clause 2 is unmeasured on free-form (F33: n=0). The landing does not sell this PRD's product: zero mentions of DMS / Spaces / spreadsheet / Excel / SQL Server; it names "Netie Cortex ... connects to your databases (SQL, NoSQL, APIs)" and says "Palantir" ten times, while section 2 says "We do not say Palantir externally" and no gate traces the SQL / NoSQL / API claim (Cortex PRD F9 already flagged the same page's Prompt-to-Agent / Ghost Mode copy). The grain guard, the n=431 bench, stream ingest and the generated brief are green and **invisible** - none is reachable from the UI or the landing, so none of it is nearer. **NEEDS-YOU (Marketing, not a DMS epic):** the landing is the one customer-facing surface in breach of section 8. |
| F49 | 2026-08-24 | 12-agent recon brief, re-verified by PRD Agent against the code | **P0 defect - there is no authentication anywhere.** `apps/api/dms_api/middleware_actor.py:1` says it in its own docstring ("T5 lite - trust headers until OIDC. Not a security boundary for production."); lines 15-23 read `x-dms-tenant-id`, `x-dms-actor-id` and `x-dms-role` straight from client-supplied headers; `app.py:119-132` registers `DevActorMiddleware` and includes 13 routers with no auth dependency on any of them (`deps.py:47-51` declares five dependencies - settings, cortex, space store, store binding, ask service - and no principal). Every route is reachable unauthenticated as the seeded steward, and the caller picks their own role. | **none - SCOPE decision, not only a ticket; founder.** No open epic owns identity, and the fix is not the same work under the two answers. `DR-0004` opened `status: proposed` in `D:\DMS\docs\decisions\` to hold the choice: **(A)** constrain the first sale to a single-tenant, VPN-only or air-gapped install and write the header/process-constant trust boundary into the SOW as a named limitation, or **(B)** OIDC lands before any install. Both change what a contract may promise, which is why neither is an agent's call. | **Re-verified by me, not taken on report** (R-0003). One nuance the brief did not state and it changes the risk order: `grep -rn "request.state" apps/ packages/ --include=*.py` returns **only the middleware that writes it** - nothing reads it - so the header path is currently **inert**, not live. That is exactly why the ordering caution holds: wiring `DevActorMiddleware` through as an interim step converts an inert A-0005 into a live privilege escalation, because `x-dms-role` is caller-supplied and unverified. **Credential verification must land in the same change as any wiring, or not at all.** Under (A) the honest slice is small: no route reads a header, actors come from server-side constants, and the limitation is contractual. Under (B) it is an identity-provider integration and is new scope. Until answered, every object-level permission item below is decoration (F55), and the governance half of the ask (F60) cannot be told to a buyer at all. |
| F50 | 2026-08-24 | 12-agent recon brief, re-verified by PRD Agent against the code | **P0 defect - KB attack A-0005 is live: the caller names the actor written to the tamper-evident ledger.** `routes/pipelines.py:32-37` declares `GoldSignBody.steward_id: str` as a plain request-body field; `:77-81` calls `compliance_gate(action="pipeline.gold_sign", ...)` passing **no actor**, so the F5 gate sees a literal default; `:84-90` hands `steward_id=body.steward_id` to `gold_sign_metric`. Combined with F49, anyone who can reach the API can sign a gold metric attributed to any steward name they type. | **Proposed EPIC-025 - "the ledger names an actor the caller could not choose, and reports its own break honestly".** Repo: `Netie-AI/dms`. Contract impact: **none** (the actor already travels inside the existing Cortex append payload; no wire moves). Depends on: nothing for the first ticket, F49/`DR-0004` for the rest. Tier: FOUNDATION/BOUNDARY, and **visible** - the Audit page is where it shows. Authorized by an existing clause, so it is not a PRD amendment: section 2 external FAQ already promises "the confirmation lands in the ledger with what changed and **who approved it**". Founder to approve opening it (F37/EPIC-024 precedent), or say the fix hangs under EPIC-003 instead. | **Re-verified by me** at `routes/pipelines.py:32-37,77-81,84-90`. **Half of this is unblocked by the scope decision and should not wait for it:** deleting `steward_id` from `GoldSignBody` and deriving the actor server-side closes the forgery even in a header-trust world, because `settings.dms_actor_user_id` is a process constant no caller can set (proved by the same "nothing reads `request.state`" grep in F49). Passing `actor=` into `compliance_gate` is the second line. The blocked half is a *real* principal rather than a process constant, and that waits on F49. **Fix this before F53 opens**, per R-0004: the action framework must never repeat the shape, and this is the live instance of it. Acceptance in customer terms: WHEN a gold metric is signed, THE SYSTEM SHALL attribute it to an identity the caller could not supply, asserted on the sign response and `GET /v1/audit/ledger`, never on the Cortex append payload. |
| F51 | 2026-08-24 | 12-agent recon brief, re-verified by PRD Agent against the code | **P0 defect - KB attack A-0007 is live: unauthenticated full-table read on two GET routes.** `routes/library.py:192-209` (`GET /v1/library/warehouse/{table}/preview`) and `:212-232` (`GET /v1/library/bronze/{table:path}/preview`) nest the Space scope check inside `if space_id:` - **omit the query parameter and the check is skipped entirely**, falling through to `warehouse_preview(table, limit=limit, offset=offset)` with `limit` up to 500. Neither route calls `compliance_gate`. The invariant meant to catch this is blind by construction: `tests/invariants/test_boundaries.py:39` sets `MUTATING_METHODS = {"post", "put", "patch", "delete"}`, so it is green on a surface it never inspects - R-0007 exactly. | **EPIC-003 (`Netie-AI/dms#6`), OPEN.** This is not new scope and needs no amendment: EPIC-003's acceptance already is that the Space boundary holds, and a boundary that holds on ask but not on preview does not hold. Section 2 internal FAQ states the same failure in the founder's own words ("Two customers in one room is a demo we cannot currently give"). **READY for epic-agent.** | **Re-verified by me** at `library.py:199` and `:219`, and the invariant's blindness at `test_boundaries.py:39`. **The trap that stops this being a one-liner, and it is a real one:** `apps/ui/src/lib/api.ts:259-293` sets `space_id` only `if (spaceId)`, so the Library page browses with no Space selected today. Making the parameter mandatory refuses legitimate work, which is a failure and not a win (R-0005) - the ticket must decide what "no Space selected" means and re-run `verify_demo_live.py` (31/31 today) before it is called done. Extending the invariant to classify by what a route **reaches** rather than by HTTP verb touches a protected path, so the commit body needs an `INVARIANT-CHANGE:` trailer, and the extended gate must be proved able to fail before it is trusted green (R-0007). |
| F52 | 2026-08-24 | 12-agent recon brief - reported, **not** re-verified by me | Four defect classes travelling with the three P0s. (a) Five further data-revealing GETs reported ungated: `/v1/library/chunks/search`, `/v1/studio/chunks`, `/v1/admin/overview`, `/v1/runs`, `/v1/audit/ledger`. (b) Ledger contract-mapping: `client.py:135` reads `first_break`/`break_at` where the contract emits `broken_at`, so a BROKEN chain reports its break location as null, and `promote.py:540` reads a non-existent `entry_hash`. (c) Ingest sync failure swallowed into a `logger.warning` while the receipt still says `ingested=N` - R-0011 on the artifact the customer receives. (d) 78 recorded no-match probes return a NULL SUM indistinguishable from a legitimate zero - hard rule 12 surviving inside the layer meant to enforce it. | (a) folds into **F51's ticket under EPIC-003** - the ticket re-derives the full list from the code rather than trusting either brief. (b) -> proposed **EPIC-025** (F50): a ledger that misreports its own break is the same defect class as one that misreports its actor. (c) -> **EPIC-008 (#8), OPEN** - an upload whose sync silently failed breaks precisely the stranger path that epic exists to make honest. (d) -> **EPIC-019 (#38), OPEN, queued** - same lane as the F46 value dictionary. | **Routed, and marked unverified on purpose.** I read the code for the three P0s above and did not read these; they are claims pending verification, and a ticket that opens with an unverified file:line is how a wrong fix gets written confidently. Each receiving ticket re-derives its own evidence first. |
| F53 | 2026-08-24 | founder ask (PRD Agent) - "Ontology as an operational surface: actions" | Operational ontology, the **actions** half: the ontology should be a surface that acts, not only one that is read. | **EPIC-005b (DMS) blocked on EPIC-005a (Cortex, contract impact additive).** Both were allocated in Wave 3 on 2026-08-02 and **neither has ever been opened as a GitHub issue**. No amendment is needed and none should be written - the PRD already promises this in section 2 external FAQ ("writes go through a proposal you confirm"). | **Routed - queued, not ready.** Verified: the seam is named and empty at `apps/api/dms_api/routes/amend.py:29-42`, and the only three occurrences of `call_action` or `tool_runner` anywhere in `apps/` or `packages/` are those comments; the shipped `/v1/ontology` is GET-only by construction (`routes/ontology.py` `SECTIONS` is an allowlist, every handler a GET, proxying a Cortex-authored ontology DMS never writes). So "ours is read-only" is accurate, and the honest slice is **EPIC-005b only, over `dms.proposal_versions` and nothing else** - not a general action framework. Non-negotiable design constraints, taken verbatim from the mechanism Foundry uses and from the two KB attacks: the model may choose an action **name** from a declared allowlist and may propose parameter **values**, but values are re-validated server-side against a typed schema and a closed operator allowlist; **no request field may carry authority** and the actor is the authenticated principal, never a body field (which is why F50 is fixed first); an action statically declares which object types it may touch and cannot write one it did not declare; and the apply route calls `compliance_gate` with `resource=` naming the target, then `enforce`, then the side effect, in one transaction with the ledger append before commit. |
| F54 | 2026-08-24 | founder ask (PRD Agent) - "writeback" | Ontology **writeback**: the system should write back into the records the ontology describes. | **none - PRD amendment; founder.** It splits three ways and they are not one decision. **(a)** Governed amend over DMS's own lake = F53/EPIC-005b, already allocated, no amendment. **(b)** Writeback into a **customer** database = the reversal the founder already declined YES in F27 (2026-08-05), reopened as **F36 NEEDS-YOU (i)** and unanswered since 2026-08-07. **(c)** A Workshop-class operational app surface with writeback backends = out of scope under section 3 `H6` / `P-DMS-30`, where option B was explicitly rejected in F29. | **STOP - not sliced.** F36's own recorded verdict already answers (b) on the architecture: "the enforcing principal becomes the customer's SQL Server, which already granted those rows to the caller, so the Space boundary degrades to advisory unless DMS holds its own narrower credential". Said plainly for the decision: **writeback inverts the only claim this product can currently sell.** PRD-001 is titled "governed answers over your own data"; its press release sells read with evidence and a gated proposal, not an operational application platform. That does not make it wrong to want - it makes it a different product line, and only the founder may say PRD-001 is that product. Note the ordering trap: (b) is worth less than nothing before F49, because a write attributed to a caller-chosen identity is worse than no write at all. |
| F55 | 2026-08-24 | founder ask (PRD Agent) - "object-level permissions" | Object-level permissions over the ontology. | **Split. Row-level half -> EPIC-003 (#6), OPEN**, blocked on F49/F50. **Column masking and the full Foundry marking / classification lattice -> DECLINED**, with the general case refused in the PRD rather than half-built. | **Routed, and the good news is that almost none of it is new code.** Verified: `dms.acl_grants` already carries the right shape (alembic `0001:150-163`), `compliance_gate` already accepts `resource=`, the manifest already carries and **signs** `row_predicates` (table -> SQL boolean) and Cortex already enforces them - so DMS adds no enforcement code and `tests/invariants/test_no_local_enforcement.py` keeps holding. What is missing is a producer: every emitter writes the literal `'TRUE'`, and `acl_grants` has zero rows and no writer. The slice is a `PostgresSessionStore` over the existing `SessionStore` Protocol reading `space_members` + `acl_grants` + `data_sources` and emitting a non-`TRUE` predicate. Acceptance on the customer envelope per hard rule 10a: **two principals, same question, different row sets, and the narrower answer says WHY it is narrow, never WHAT was excluded.** The decline of the lattice is not laziness: the cost there is the interaction matrix and the multi-datasource-per-object-type case, which is where a small team ships a security hole rather than a bug. Pick one enforcement point - a non-`TRUE` `row_predicate` on the already-signed manifest - and refuse the general case. **Every step of this is decoration until F49 is answered**, because a predicate keyed to an identity the caller chooses enforces nothing. |
| F56 | 2026-08-24 | founder ask (PRD Agent) - "apps built on it" | Apps built on the ontology (Workshop / OSDK / app authoring parity). | **DECLINED.** Foreclosed twice and the founder has already ruled: section 3 lists Workshop / Contour / Quiver / Cipher / OSDK / Agent Studio / Pipeline Builder / writeback / media backends as out of scope under `H6`, and F29's recorded disposition is "B rejected; `P-DMS-30` stays parked". | **Declined, and the lawful path already exists and is gated.** EPIC-022 is the only generative-app route in this PRD and its gate is EPIC-018 reporting 100.00 pct precision-on-answered across two consecutive waves at coverage >= 60 pct. That is not met and is not close: STATUS records coverage low, everything answered coming from L0/L1 rather than L2, and #57 open as a confidently-wrong answer. The gate's own stated reason applies exactly here - generating apps over an unverified metric layer mass-produces confidently wrong dashboards. This is not a scheduling question and reopening it is not a scheduling answer. |
| F57 | 2026-08-24 | founder ask (PRD Agent) - "strong AIP logic building" | AIP-style LLM logic building: a block-based builder for model-driven logic with a debugger and an eval studio. | **DECLINED as a product. The mechanism is adopted free under F53 at no cost and no epic.** | **Declined, and this is the decline I am most confident about.** F31 already routed the same shape ("no new DMS epic; do not invent a sixth DMS port") and the PRD's governing sentence stands: **Cortex stays the only orchestrator.** `DR-0003` pre-rejected both AIP-Logic-shaped implementations by name - prompt engineering and request-time LLM-as-judge - and chose deterministic compile-time validation with executed post-conditions. The builder is a product company; the mechanism underneath it is three sentences and is already the whole A-0005 defence: **the model's only write path is the same Action a human uses, tools are ontology-typed, and a function statically declares which entities it may edit.** Copy the mechanism into F53's constraints, which is done above. Decline the builder. |
| F58 | 2026-08-24 | founder ask (PRD Agent) - "storage + compute at TB level" | Storage and compute at the terabyte tier. | **No new epic. EPIC-020 (`sql_source` extract to bronze) already owns the ingest half and is BLOCKED on F36**; F43 already routed `scripts/stream_ingest.py` there. The loader ceiling is parked as **`P-DMS-34`**, unlock condition: a named customer table that exceeds it. | **Routed - and the first thing that breaks at TB is not the engine.** Reported and not re-verified by me: `scripts/load_adventureworks.py:402` does `pd.read_sql('SELECT * FROM ...')` with a measured 19.1x RAM amplification over on-disk Parquet, capping a single table near 700 MB of Parquet on a 13.67 GB host, with no chunk size to raise. Query speed is not the constraint at any size the product has met - a 3-table join over 121,317 x 31,465 rows ran in 0.260s. **The honest ceiling is concurrency, and it is hit at 91 rows, not at a terabyte:** one DuckDB writer excludes all readers, reproduced in-process and cross-process, so one Studio upload overlapping one chat ask is enough. That is a real defect in a shipped product and it is worth more than the tier. It has no PRD clause naming concurrency, so it parks with `P-DMS-34` rather than manufacturing an epic - but it should be said out loud before anyone demos two browser tabs. |
| F59 | 2026-08-24 | founder ask (PRD Agent) - "at PB level" | Storage and compute at the petabyte tier. | **DECLINED outright, and separately a ban on the claim.** No epic, no park, no "PB-ready" language in any external asset. | **Declined on two independent grounds, either of which is sufficient.** Capability: the four things one node genuinely cannot do - repartitioning shuffle over a network, task-level retry over spooled intermediates, elastic workers, a distributed cache tier - are thousands of engineer-years deep in Spark, Trino and StarRocks, are free, and DMS cannot reach an object store at all (httpfs is not in its DuckDB build; `reveal.py:17` exists only to refuse `s3://` and `azure://`). `D:` is a 3,815 GB exFAT volume, so a petabyte is about 260x the only storage device this code can address. Claims: the **product has served 91 rows** across 19 tables in an 11,022,336-byte DuckDB file. The 1,812,007-row lake that was loaded offline is **never read by `apps/` or `packages/`** - `read_parquet` appears there only on the hostile-SQL blocklist. Section 8 binds every external asset to a passing gate, and F48 already has a NEEDS-YOU open against the landing for breaching it. **Any petabyte sentence in any external asset is a section 8 breach on the day it is written.** |
| F60 | 2026-08-24 | founder ask (PRD Agent) - "governance" | A governance surface of the Unity-Catalog kind: catalog, RBAC, lineage, audit, as a platform capability. | **DECLINED as a catalog product.** The governance this PRD already promises is F49, F50, F51 and EPIC-003 - and none of it holds today. | **Declined, and this is the row to read twice.** The ask names governance as something to add. The measurement says governance is the thing currently missing from what is already sold: there is no authentication anywhere (F49), the ledger takes its actor from the request body (F50), and two GET routes return up to 500 rows of any table with no gate and no scope (F51). Building a catalog surface on top of that would be a governance story told over an API that has no principal. **Fix F49, F50 and F51 before telling anyone a governance story at all** - that sequencing is the whole content of this row. Rebuilding the securable hierarchy, governed tags, agentic classification and ABAC policy evaluation is a multi-year build against software that carries no line item on a Premium SKU and is four funded vendors deep (Polaris, Unity, Lakekeeper, Glue). |
| F61 | 2026-08-24 | founder ask (PRD Agent) - "ML" | An ML platform: training, feature store, model registry, experiment tracking, serving. | **DECLINED without qualification.** | **Declined; it is a documented architecture violation before it is a schedule problem.** `packages/core/dms_core/ports.py` declares exactly five ports and `ModelProviderPort` is a single `complete(prompt)` protocol; CLAUDE.md hard rule 6 forbids a sixth port without a stated swap scenario, and there is no swap scenario here because there is zero code. Every layer of this is owned and priced near zero - MLflow free, SageMaker AWS-native, Vertex GCP-native. NETIE.md section 2 already settled the class: we buy tokens on plane 1, we do not build there. The only model-shaped work this product should own is schema and vocabulary mapping, value normalization, interpretation ranking and abstention calibration - all features of the semantic layer, none of which needs a training platform. |
| F62 | 2026-08-24 | founder ask (PRD Agent) - "notebooks" | A notebook product inside DMS. | **DECLINED.** | **Declined, because it is the exact inverse of the product thesis rather than merely expensive.** The entire claim is that every figure is compiled, cited, conserved and refusable. A notebook is an ungoverned arbitrary-code surface where a user computes any number with no provenance and no E9 - shipping one would let a customer manufacture, inside DMS, precisely the uncited figure the envelope validator exists to forbid. It is also unsellable: Jupyter is infrastructure, `.ipynb` is the interchange format, marimo is free and better on reactivity, and a proprietary notebook format is a lock-in liability rather than a moat. The maximum honest offering is an `.ipynb` export plus an endpoint the customer's existing notebook calls, and that is not an epic today. |
| F63 | 2026-08-24 | founder ask (PRD Agent) - "jobs" | A jobs / scheduler / DAG surface. | **DECLINED as a scheduler.** The one differentiated sliver - "this scheduled answer is governed, cited, and in the ledger" - has no PRD clause and no customer asking for it, so it parks as **`P-DMS-35`**, unlock condition: a customer names a recurring answer they need to land in the ledger. | **Declined.** Airflow, Prefect, Dagster and Temporal are funded, managed-hostable in the low three figures per month, and have hiring pools; writing a DAG engine is one to two engineer-years for a component that is free. The real need hiding inside "jobs" is a nightly refresh of five files, which is one CLI entry point plus the OS scheduler. Building the thin governed record over someone else's cron is the only part worth anything, and it is worth nothing before F49 - a scheduled write with no principal is an unattributed write on a timer. |
| F64 | 2026-08-24 | founder ask (PRD Agent) - "improve Delta's ACID + time travel" | Improve on Delta Lake's ACID guarantees and time travel. | **DECLINED as a table format.** `P-DMS-17` already settled this correctly: "DMS sits on top of the warehouse, not as a second warehouse engine." The ACID-shaped item that is real is **atomic ingest visibility**, already routed as F43 -> EPIC-020. | **Declined on the mechanism, not on the ambition.** Almost everything hard in Delta's design exists to solve multi-writer coordination on an eventually-consistent object store - a problem DMS does not have, having no Spark, no Delta, no Iceberg and no second concurrent writer. A single Postgres already gives strictly stronger isolation at row grain than Delta's default WriteSerializable. The format war is also settled: Iceberg won on governance, Delta owns Databricks, Hudi owns update-heavy streaming, and the value moved up to the catalog layer. What **is** a live defect and is worth the attention this ask was pointing at: bronze ingest is drop-and-replace (`bronze.py:225-226`) and destroys the prior version, while every bronze row already carries `_ingest_id` and `_src` that nothing reads. Take the one piece a table format structurally cannot do - see F65. |
| F65 | 2026-08-24 | PRD Agent counter-proposal, from the same intake | **Semantic versioning of the answer:** hash the ontology definition through `canonical_manifest_bytes` into the same chain as the answer, carry `(ontology_version, data_version, question_hash)` on the envelope and one new ledger event, and add a replay gate that exits 1 naming the measure that moved - so an old figure can be replayed under **the definition that produced it**. | **none - PRD amendment; founder. Downstream of F41, which is still open.** Contract impact **additive**, so it may not be one epic: it splits Cortex engine change -> contract bump and spec regeneration -> DMS adopts the field, with hard blocking, per the contract-stability rule. | **Drafted, not opened, and deliberately sequenced last of the ontology items.** This is the one genuinely unoccupied position the intake found: Delta, Iceberg, Hudi, Nessie and lakeFS all version **bytes and schema** and none versions **meaning**, because they sit below the semantic layer by design and cannot know what "revenue" means. But it is worth nothing before the compiler it would version is in the product, and that is F41, open since 2026-08-23. Bound the claim when it is eventually made: complete only over what the compiler emits, with an explicit provenance-unknown marker for everything else - a version hash that silently covers only part of the definition is a worse lie than no hash. |
| F66 | 2026-08-24 | founder ask (PRD Agent) - "Unity Catalog's lineage/RBAC surface" | Improve on Unity Catalog's lineage and RBAC surface. | **DECLINED as a catalog build.** The RBAC half is F55; the lineage half is F67. The one sentence worth keeping out of this whole comparison is a **Marketing** asset, not a roadmap item. | **Declined, with the asset named so it is not lost.** Unity Catalog's lineage is an execution-event stream shipped from the compute plane over a bus - by Databricks' own documentation a subset of read/write events, severed by a rename, degraded through UDFs and path-referenced sources, and structurally absent for code that never ran. **It is a log of what happened.** A compiled ontology knows the full column-level dependency set at plan time for every expressible query, including ones nobody has run - that is a proof, not telemetry, and it is a categorically different object rather than a better version of the same one. The marketing sentence already sitting in this repo: `scripts/load_adventureworks.py:30-34` records that Databricks' `rely.at_most_one_match` is **declared and, by their own documentation, not validated at runtime**, so a fan-out silently returns incorrect measures, while `validate_lake.py` runs the check and fails loudly. Section 8 still binds it: that sentence traces to a gate on an **unmerged branch** (PR #68), so it may not appear externally until the branch decision lands. |
| F67 | 2026-08-24 | PRD Agent counter-proposal, from the same intake | **Compiled lineage rendered from the receipt that already exists:** `source_rows -> passed + quarantined`, `counts_by_reason`, and `unmatched` (negative means join fan-out), shown on the surface the customer already opens. | **EPIC-024 (proposed under F37 on 2026-08-07, still awaiting founder).** Do **not** open a second lineage epic - this is the same epic arriving by a second route, which is precisely what the ledger exists to prevent. | **Routed to the existing proposal, and it is now the cheapest visible thing on this list.** The data is computed on every promote and thrown away: `PromoteReceipt.to_dict()` returns all of it, `POST /v1/pipelines/run` returns it verbatim behind `compliance_gate`, and `apps/ui/src/lib/api.ts` contains zero pipeline functions - nothing in the UI has ever called it. F37 already established the surface (Library, not a sixth page) and the discipline (do not build an arrow diagram - dbt and Airflow render the arrow for free; nobody renders `reconciled: false` with the rows that never arrived). **It is also the natural human-inspectable half of the WIP pair** for whichever invisible foundation epic runs next, which is the reason to approve or reject it now rather than leave it open a third week. |
| F68 | 2026-08-24 | founder ask (PRD Agent) - "help find money, ensure we can earn money" | **Monetization.** How the product is priced, metered, licensed and invoiced. | **none - PRD amendment; founder. Genuinely new scope with zero prior coverage.** | **STOP - and this one is not a routing failure, it is a real gap.** Verified by search over PRD-001: **zero** occurrences of monetis, monetiz, billing, subscription, revenue or invoice; the single `pricing` hit at line 413 is an Azure AI Foundry competitor mention and the single `SKU` hit is inside F41. Section 3 also **forecloses the usual shape** - "Multi-tenant hosting - self-host and single-tenant pilot only" - so a metering or plan surface contradicts an existing out-of-scope clause and is an amendment rather than a new epic under an existing one. The only adjacent artifacts all gate on the same unfired event: `P-DMS-5` (usage caps) on "first paying conversation", EPIC-009 and `P-DMS-16` on the same, and the dms version line moves 0.1.0 -> 1.0.0 at first paying install. **Argue the decision against the two real alternatives, not against Foundry:** the Power BI licence the buyer already owns with Copilot bundled, and one more analyst at RM 4,500-7,000 per month plus 14-16 pct statutory on-cost. One monetization-shaped item needs no product change and no amendment and is the fastest cash on this entire list: **a packaged adversarial refusal audit sold as a report** - it needs no deployment, and the R-0010 methodology limit must be printed on its face. Still a founder call, because it is a commercial commitment. Open question worth settling first: per-verified-metric pricing has no viable denominator until the grain guard has met one dirty customer schema - if the answer is six metrics rather than twenty-five, that SKU collapses. |
| F69 | 2026-08-24 | nearness check - every run (PRD Agent) | Artifacts checked this run: `D:\DMS\STATUS.md` (2026-08-24); `apps/api/dms_api/middleware_actor.py`, `app.py:110-134`, `deps.py:47-51`, `routes/pipelines.py`, `routes/library.py`, `routes/ontology.py`, `routes/amend.py`, `tests/invariants/test_boundaries.py`, `apps/ui/src/lib/api.ts`; `apps/ui/src/pages` = 10 app pages; `git rev-list --left-right --count main...feat/ontology-grain-guard` = **0 / 18** with `git ls-tree main scripts/` carrying none of `ontology.py`, `ontology_bench.py`, `insights.py`, `brief.py`, `load_adventureworks.py`, `validate_lake.py`, `stream_ingest.py`; `gh pr view 68` = **OPEN**; `gh issue list --label epic` = **7 open**. The landing was **not** re-fetched this run - F48's measurement stands and its NEEDS-YOU is still open. | **none - measurement only.** | **Measured; no percentage. Not nearer than F48, and on one axis further.** Four reasons. (1) The app path and the demo are unchanged: 31/31 on a cold stack, ten pages, same surfaces a buyer would open. (2) F48 recorded the grain guard, the n=431 bench, stream ingest and the generated brief as green and **invisible**; they are still on an unmerged branch. PR #68 turns that from a discovery into a review, which is process progress and **not** nearness - nothing a buyer can open changed. (3) Three P0 defects are now known where before they were unknown. Knowing is strictly better than not knowing, but the honest reading is that the product a buyer would be handed is **further from deliverable than the previous measurement implied**: the governance half of the pitch cannot be told over an API with no authentication, where the ledger takes its actor from the request body and two GET routes return up to 500 rows of any table unscoped. (4) The landing still does not sell this PRD's product (F48, unchanged, Marketing-owned). The single highest-nearness item on this entire intake is **EPIC-024** (F67/F37) - it renders data the product already computes, on a page that already exists, and it has been waiting on a yes or a no since 2026-08-07. |

---

## 8. Review

Silent read, 15 minutes, before slicing. The FAQ's internal section is the part to read
properly - the external section is the part we cannot yet fully support.

**Nothing in the press release may appear in any external asset until it traces to a
passing gate.** Today, the quote and the drillthrough claim would both be premature.

