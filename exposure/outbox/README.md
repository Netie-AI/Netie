# Generated drafts. Re-run: python -m netie_exposure queue --offline

This folder is filled by `queue`. Seed copies for the first week live in `seed/`.
Nothing here is posted until a human runs `python -m netie_exposure approve <id>`.
